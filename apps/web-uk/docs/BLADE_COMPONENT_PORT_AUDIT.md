# Blade Component Port Audit

Last reviewed: 2026-07-10

## Purpose

This audit lists reusable Laravel Blade accessible frontend patterns that should
be ported into `apps/web-uk` while preserving the Express/Nunjucks/GOV.UK
Frontend stack.

## Ported Or Started

| Blade pattern | Laravel source | ASP.NET target | Status |
| --- | --- | --- | --- |
| Custom dark header | `accessible-frontend/views/layout.blade.php` | `src/views/layouts/base.njk`, `src/lib/accessible-shell.js` | Started. Text brand, language selector, My account link, and service navigation are present. The no-JS language selector now mirrors Blade's `request()->except(['locale'])` behavior for scalar query params, preserving filters/status/return values while excluding `locale`. |
| Localization runtime, catalogs, formatting, and RTL | Laravel `lang/{locale}/govuk_alpha*.php`, user preferred-language profile, and Blade document direction | `src/lib/localization`, `src/middleware/localization.js`, `src/lib/request-locale-context.js`, `src/lib/request-profile.js`, `src/lib/request-intl-locale.js`, `src/lib/api.js`, `src/views`, `src/assets/scss/main.scss` | Started. The generated catalogs cover 11 locales, 24 namespaces, and 7,337 keys per locale with zero structural drift. Query/session/request-profile/signed-token-profile/weighted-header selection, request-scoped propagation, shared profile reads, locale-aware formatting, tenant-scoped profile choices, `Content-Language`, document `lang`/`dir`, and Arabic RTL are wired. A conservative audit applied 1,595 semantically safe substitutions across 257 templates and now reports zero remaining safe exact matches across 290 templates. Explicit contextual work covers all nine public browser-gate titles/H1s, exact auth route statuses/errors, scoped dynamic accessible labels, all 19 Explore cards/page chrome, and 45 exact profile/settings status keys. This is not complete translation coverage: every non-English authoritative Laravel catalog is still 53.2%-53.9% English-identical, 16 entire namespaces remain English in the read-only source, and hundreds of contextual titles, headings, validation/status messages, ARIA labels, residual templates, broader RTL states, and manual review remain open. Full Jest (31 suites, 1,021 tests), lint, CSS, and the 12-case browser gate passed. |
| Document semantics and browser accessibility gate | `accessible-frontend/views/layout.blade.php`, representative public Blade pages | `src/views/layouts/base.njk`, `playwright.accessibility.config.js`, `scripts/accessibility-local-server.js`, `tests/accessibility/public-pages.spec.js` | Started. The shared layout now renders exactly one parent GOV.UK `main` landmark and one `main-content` ID. `npm run test:accessibility` starts the current checkout on an OS-assigned port and runs Chromium plus axe against shared-mount pages, asserting successful responses, one main/h1, unique IDs, horizontal reflow, and no serious/critical axe violations. The expanded 2026-07-10 gate passed 12/12: nine representative public pages plus three Arabic RTL pages at 320px. The first expanded run exposed the registration honeypot's physical off-screen overflow; the focused regression and full rerun passed after it moved to a clipped technique. An app-wide source regression also covers all 135 Nunjucks error summaries; the six missing `tabindex="-1"` focus targets were fixed and zero violations remain. Authenticated browser error states, uploads, destructive actions, additional RTL coverage, Firefox, WebKit, and recorded manual assistive-technology coverage remain uncertified. |
| Header visual layer | `accessible-frontend/src/app.scss` | `src/assets/scss/main.scss` | Started. Dark header, accent strip, language form, nav badge, card list, and link-button patterns are present. |
| Service navigation | `AlphaController::alphaNavItems()` | `src/lib/accessible-shell.js` | Started. Header labels now mirror Blade IA. The shared shell now gates Dashboard, Feed, Listings, Members, Events, and Volunteering against tenant bootstrap `modules`/`features`, matching Laravel's `TenantContext::hasModule()` and `hasFeature()` service-nav semantics. Page-level disabled-state redirects/errors still need module-by-module certification. |
| Footer columns | `AlphaController::alphaFooterColumns()` | `src/lib/accessible-shell.js` and `partials/footer.njk` | Started. Platform, Support, and Legal columns mirror Blade labels and local path equivalents. The Platform column now filters Listings, Members, Events, Volunteering, and Blog by tenant bootstrap module/feature flags, and drops the column when no platform links are enabled. |
| Footer meta | `layout.blade.php` | `partials/footer.njk` | Started. Report problem, Cookies, POST sign-out, AGPL attribution, and source link are present. The visually-hidden meta heading now uses Laravel Blade's `Supporting information and attribution` wording. |
| Generated prep fallback guard | Laravel accessible route matrix | `src/routes/laravel-prep-pages.js` | Started. The loader now registers generated preparation pages only for matrix rows explicitly marked `missing`. The current 608/608 route matrix leaves the runtime loader with `0` prep pages, so matched Laravel GET routes are served by real route modules rather than generic skeleton handlers. Future prep pages remain route-discoverability evidence only and do not certify Blade visual/workflow parity. |
| Shared pagination | Laravel Blade paginators and GOV.UK pagination pattern | `src/views/partials/pagination.njk` | Started. The shared Nunjucks partial builds GOV.UK pagination links from a supplied `paginationConfig.baseUrl`; its documented/default members fallback now uses `urlFor('/members')` instead of raw `/members`, with source regression coverage so omitted defaults stay tenant-aware. Focused members-index render coverage, full Web UK Jest, lint, route matrix, and a scoped live Laravel `/members` smoke passed. Visual parity and every pagination caller still need module-by-module certification. |
| Shared empty states and breadcrumbs | Laravel Blade empty-state links and breadcrumb patterns | `src/views/partials/empty-state.njk`, `src/views/partials/breadcrumbs.njk` | Started. Empty-state primary and secondary action hrefs now render through `urlFor()`, and breadcrumb examples use `urlFor(...)` paths. Focused source regression, tenant-prefixed members no-results render coverage, full Web UK Jest, lint, route matrix, and a scoped live Laravel `/members` smoke passed. Visual parity and every empty-state/breadcrumb caller still need module-by-module certification. |
| AI chat redirects | Laravel accessible AI assistant routes | `src/routes/ai-chat.js` | Started. Auth-required, empty-message, and post-send redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. The unmounted legacy `src/routes/chat.js` router was removed, so source audits only inspect the mounted Laravel-compatible chat route. Focused source regression, shared-mount empty-message POST redirect coverage, focused AI chat render/redirect tests, full Web UK Jest, lint, route matrix, and a scoped live Laravel `/chat` smoke passed. Full AI assistant workflow parity, persistence depth, localization, visual parity, and ASP.NET backend compatibility are not certified. |
| Matches redirects | Laravel accessible matches routes | `src/routes/matches.js` | Started. Match dismiss and board dismiss redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression, shared-mount match-dismiss POST redirect coverage, full Web UK Jest, lint, route matrix, and scoped live Laravel `/matches` and `/matches/board` smoke passed. Full recommendation workflow parity, persistence depth, localization, visual parity, and ASP.NET backend compatibility are not certified. |
| Auth redirects | Laravel accessible auth routes | `src/routes/auth.js` | Started. Login, two-factor, register, logout, forgot-password, and reset-password redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression, shared-mount login POST redirect coverage, full Web UK Jest, lint, route matrix, scoped live Laravel auth smoke, and repeatable `npm run visual:blade` public auth checks for login/register/forgot-password/reset-password passed. Full Laravel credential viability beyond the smoke fixture, two-factor persistence, registration delivery, password reset email delivery, localization, full visual parity, and ASP.NET backend compatibility are not certified. |
| Member onboarding redirects | Laravel accessible member onboarding routes | `src/routes/onboarding-posts.js` | Started. Auth-required, step, avatar, validation, safeguarding, completion, and dashboard handoff redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression and scoped Laravel completed-user redirect smoke passed. Full onboarding visual parity, persistence edge cases, localization, runtime certification, and ASP.NET backend compatibility are not certified. |
| Server-level redirects | Laravel accessible shell/account/cookie/organisation routes | `src/server.js` | Started. Deterministic cookie, account, and organisation redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression, shared-mount cookie and organisation redirect coverage, full Web UK Jest, lint, route matrix, and a scoped live Laravel cookie/organisation smoke passed. User-provided safe return URLs, every redirect family, full organisation workflow persistence, localization, visual parity, and ASP.NET backend compatibility are not certified. |
| Contact/support redirects | Laravel accessible contact and report-a-problem routes | `src/routes/contact-support.js` | Started. Contact validation/result redirects, signed-out report-a-problem handoff to contact prefill, unsigned report POST redirects, and report validation/sent/failed redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression, shared-mount contact/report redirect coverage, and repeatable `npm run visual:blade` public contact/report-a-problem checks passed. Full support-report persistence, Laravel rate-limit/error variants, localization, full visual parity, and ASP.NET backend compatibility are not certified. |
| Notifications redirects | Laravel accessible notifications routes | `src/routes/notifications.js` | Started. Grouped-read, read-all, delete-all, single-read, single-delete, API-error, and validated return redirects now go through a `res.locals.urlFor` helper instead of raw root-relative redirect calls. Focused source regression and existing notification alias behavior coverage passed. Realtime delivery, unread-count depth, localization, visual parity, and ASP.NET backend compatibility are not certified. |
| Tenant home page | `views/home.blade.php`, `AlphaController::home()` | `src/server.js`, `src/views/home.njk`, `src/lib/api.js`, `src/assets/scss/main.scss` | Partial. Tenant-root contexts now render the Blade-style `Accessible` home page with community caption, tenant tagline, guest/signed CTAs, beta/accessibility panel, tenant-scoped stat grid from Laravel `/api/v2/platform/stats`, module availability rows from tenant bootstrap modules/features, and service details. Shared mount `/{tenantSlug}/accessible` passes `X-Tenant-Slug`; dedicated custom-domain root `/` passes the resolved Host and Origin and preserves slugless links. Master and cluster custom-domain roots can render Laravel SEO h1/intro text plus `tenant_switcher` communities as a network landing, converting same-host community URLs to relative paths and preserving external domains. Focused Jest, full Web UK Jest, lint, route matrix, direct live Laravel bootstrap, focused host-root runtime smoke, repeatable `npm run visual:blade` tenant/home and host-root checks, and scoped live Laravel smoke for `/hour-timebank/accessible` are green; the local live proof rendered the `hour-timebank` tenant stats `946`, `1,988`, `129`, and `1`. Remaining gaps: exact feature-gate semantics for every tenant module, tenant logo/colour depth, localization, full visual/manual Blade parity depth, and ASP.NET backend compatibility are not certified. |
| Cookie banner and settings | `partials/cookie-banner.blade.php`, `views/cookie-settings.blade.php`, `CookieSupportParity::storeCookieConsent()` | `partials/cookie-banner.njk`, `src/views/cookie-settings.njk`, `/cookie-consent` | Partial. The banner appears before the skip link until `nexus_accessible_cookie_consent` is set, while legacy Laravel `nexus_alpha_cookie_consent` values are still accepted as a read-only dismissal fallback; `/cookies` renders the analytics yes/no settings form; POST stores local `all` or `essential` values under the cleaner accessible cookie name. Cookie-settings form action, legal-cookie-policy link, cookie-banner `/cookies` links, and banner accept/reject `/cookie-consent` POST actions now use `urlFor()` with public/auth/support and public-info/legal/support/cookie source regression coverage. The default Laravel runtime smoke now certifies the no-JS banner reject, banner accept, and settings-save redirects plus the expected `nexus_accessible_cookie_consent` cookie values against a tenant-aware local web-uk process, and `npm run visual:blade` now checks the shared-mount `/cookies` page against Laravel Blade. Laravel `cookie_consents` audit persistence, tenant scoping, localization, and ASP.NET backend compatibility are not certified. |
| About, guide, features, and FAQ pages | `views/about.blade.php`, `views/guide.blade.php`, `views/features.blade.php`, `views/faq.blade.php`, `AlphaController::about()`, `guide()`, `features()`, `faq()` | `src/routes/public-info.js`, `src/views/public-info/about.njk`, `guide.njk`, `features.njk`, `faq.njk` | Partial. `/about` renders the Blade-style community intro, four-step how-it-works list, values, contributor credits, open-source links, and CTA group; `/guide` renders the Blade-style timebanking explanation, equal-time principle, three-step ordered list, getting-started copy, and CTA group; `/features` renders the Blade-style feature list and guide CTA; `/faq` renders the Blade-style five-question GOV.UK accordion. About, Guide, Features, FAQ, Help, Trust and safety, Accessibility, Legal, and Privacy local CTAs/links now route through `urlFor()` with source regression coverage, and `npm run visual:blade` now checks those shared-mount public/legal pages against Laravel Blade markers. Tenant-domain routing, module-gated CTA visibility, localization, live platform stats, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Explore gateway | `views/explore.blade.php`, `Api\ExploreController::index()` | `src/routes/explore.js`, `src/views/explore.njk`, `src/lib/accessible-shell.js` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel `/api/v2/explore` and render the Blade card list plus listing/event sections when present. The shared Explore card list now follows the Blade candidate gates for AI assistant, Polls, Groups, Goals, Organisations through Volunteering, Blog, Resources, Marketplace, Jobs, Courses, Podcasts, Coupons, Premium, Ideation, and Federation; Search remains card-visible like Blade even when the search feature flag is false; Skills remains ungated; Exchanges require the listings module plus exchange workflow config; and Clubs use Laravel-backed active-club evidence from the clubs API probe rather than a static flag. Listing/event links, view-all links, and auth-required route redirects now route through `urlFor()`/`res.locals.urlFor`. Focused Jest/source tests and scoped Laravel runtime smoke for signed `/explore=>Explore` are green. Exact recent-listing source parity, live disabled-tenant broker-workflow proof, localization, visual/manual parity, and broader runtime behavior are not certified. |
| AI chat | AI assistant accessible route and chat workflow | `src/routes/ai-chat.js`, `src/views/ai-chat/index.njk`, `src/lib/api.js` | Partial. Signed `/chat` renders the Web UK AI assistant candidate and preserves conversation query state. The chat back link, new-conversation link, conversation links, and chat form action now route through `urlFor()` with source regression, focused render coverage, and scoped Laravel runtime smoke, so shared tenant mounts and custom-domain roots do not leak flat `/chat` or `/explore` targets. Exact Blade markup, conversation persistence depth, tenant feature gates, localization, and ASP.NET backend compatibility are not certified. |
| Matches | Matches accessible routes and board workflow | `src/routes/matches.js`, `src/views/matches/index.njk`, `src/views/matches/board.njk`, `src/lib/api.js` | Partial. Signed `/matches` and `/matches/board` render Laravel-backed matches candidates with filters, listing/event/group targets, board navigation, dismiss forms, empty states, and back links. Those local links/forms now route through `urlFor()` with source regression, focused render coverage, and scoped Laravel runtime smoke. Match dismiss and board dismiss redirects also route through `res.locals.urlFor`, with focused shared-mount redirect coverage and scoped live Laravel smoke. Exact Blade markup, recommendation/dismiss persistence depth, tenant feature gates, localization, and ASP.NET backend compatibility are not certified. |
| Dashboard | `views/dashboard.blade.php`, `AlphaController::dashboard()` | `src/routes/dashboard.js`, `src/views/dashboard/index.njk` | Partial. Signed `/dashboard` redirects unsigned visitors to `/login`, calls Laravel-compatible profile, onboarding status, wallet balance, gamification profile, badges, listings, feed, member events, exchange-attention count, and member endorsements helpers, and renders the Blade-style welcome, onboarding banner, exchange-attention banner, create-listing CTA, time-bank stats, progress/badges, upcoming events, skill endorsements, quick links, and recent feed/listings. Dashboard local links now route through `urlFor()` with source regression, focused render coverage, and scoped Laravel runtime smoke for tenant/shared-mount/custom-domain contexts. Tenant/module/feature gates, exact localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Advanced search and saved-search delete | `views/search-advanced.blade.php`, `views/search-saved-delete.blade.php`, `SearchParity::searchAdvanced()`, `searchDeleteSavedConfirm()`, `Api\SearchController` | `src/routes/search.js`, `src/views/search/advanced.njk`, `src/views/search/saved-delete.njk`, `src/lib/api.js` | Partial. Signed `/search/advanced` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel `/api/v2/search` with Blade advanced filters and `/api/v2/search/saved`, and renders Blade-style filters, saved-search actions, status banners, result tabs, and listing/member/event/group cards. Signed `/search/saved/{id}/delete` reads Laravel `/api/v2/search/saved` and renders the Blade-style destructive confirmation form. Simple and advanced search source templates route search forms, result tabs, result links, empty-state CTAs, pagination base URL, and saved-search forms through `urlFor()` with source regression coverage, focused render tests, and focused Laravel runtime smoke for signed `/search/advanced?q=garden`. Search auth and saved-search save/delete/run redirects now route through `res.locals.urlFor` with source regression coverage. Feature gates, tenant behavior, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Knowledge base | `views/kb-index.blade.php`, `views/kb-article.blade.php`, `AlphaController::kb()`, `AlphaController::kbArticle()` | `src/routes/kb.js`, `src/views/kb/index.njk`, `src/views/kb/article.njk`, `src/lib/api.js` | Partial. Public `/kb` and `/kb/{id}` call Laravel-compatible knowledge-base APIs and render Blade-style caption, search form, article cards, cursor load-more, article body, metadata, and related links. The real `/kb` source templates now route the search form action, article links, load-more link, article back link, and related-article links through `urlFor()` with source regression coverage and focused render tests. The unmounted legacy `src/views/knowledge-base` compatibility templates are also tenant-safe through `urlFor()`, but are not Laravel route-matrix evidence. Tenant feature gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Saved items, collections, and appreciation | `views/saved.blade.php`, saved collection/social Blade parity views, saved/appreciation controllers | `src/routes/saved-social.js`, `src/routes/saved-collections.js`, `src/views/saved/index.njk`, `src/views/saved-collections/*.njk`, `src/views/saved-social/*.njk`, `src/lib/api.js` | Partial. Signed `/saved`, `/me/collections`, `/me/collections/{id}`, `/users/{id}/collections`, and `/users/{id}/appreciations` render Laravel-backed saved item, collection, public collection, and appreciation pages through Laravel bookmark, collection, public collection, and appreciation APIs. Source templates now route saved filters, dynamic item links, bookmark removal, collection list/detail links, pagination, collection CRUD/item-remove forms, public collection links, appreciation send/reaction forms, member profile links, and appreciation pagination through `urlFor()` with source regression coverage, focused render tests, and focused Laravel runtime smoke for `/saved`, `/me/collections`, and `/users/14/appreciations`. Route-level saved collection and saved social redirects now use `res.locals.urlFor` for auth handoffs and POST outcomes, with focused shared-mount coverage for collection and appreciation redirects under `/acme/accessible`. Tenant feature gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Goals | `views/goals*.blade.php`, `AlphaController::goals*()`, goals parity routes | `src/routes/goals.js`, `src/views/goals/*.njk`, `src/lib/api.js` | Partial. Signed `/goals`, templates, discover, buddying, edit, check-in, reminder, buddy-action, history, insights, and social pages render Laravel-backed Blade-style candidates through Laravel v2 goals, comments, and like APIs. Goals source templates now route browse/detail links, template filters/use forms, discover/buddying controls, edit/delete forms, check-in/reminder/buddy-action forms, history/insights links, and social like/comment/reply/delete controls through `urlFor()` with source regression and focused render coverage. Tenant feature gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Connections network | `views/connections-network.blade.php`, `ConnectionsMatchesParity::connectionsNetwork()`, `Api\ConnectionsController` | `src/routes/connections.js`, `src/views/connections/index.njk`, `src/views/connections/network.njk`, `src/lib/api.js` | Partial. Signed `/connections` and `/connections/network` redirect unsigned visitors to `/login?status=auth-required`; `/connections/network` calls Laravel `/api/v2/connections` for accepted, pending-received, and pending-sent sections, calls `/api/v2/connections/pending` for counts, filters the loaded rows by name/location, and renders Blade-style status banners, search, tabs, card sections, connected-since text, empty states, load-more links, and action forms. The local `/connections/{id}/accept|decline|remove` POST handlers now call Laravel v2 connection endpoints and use Laravel status redirects. Connections index/network local navigation, member links, pagination, and action targets now use `urlFor()` with source regression coverage; connection route-level auth and POST result redirects now also use `res.locals.urlFor` so they stay inside tenant mounts and custom-domain paths. Tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation hub | `views/federation.blade.php`, `partials/federation-nav.blade.php`, `FederationV2Controller::status()`, `partners()`, `activity()` | `src/routes/federation.js`, `src/views/federation/index.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation status, partners, and optional activity endpoints and render Blade-style status banners, stats, partner preview, activity, and quick links. The hub service navigation, opt-in/opt-out CTAs, partner preview links, view-all link, and quick links now route through `urlFor()` with source regression, focused render, and scoped Laravel runtime-smoke coverage. The opted-out CTA now enters `/federation/onboarding`, matching current Laravel. Federation GET route-level redirects now use `res.locals.urlFor`, with focused shared-mount coverage proving `/acme/accessible/federation` redirects to `/acme/accessible/login?status=auth-required`. Laravel `403` from the optional activity feed now renders an empty activity list instead of a `503` page, matching the local E2E fixture's permission-limited activity state. Federation depth pages, tenant policy, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation tenant URL helpers | Federation browse, messaging, settings, transfer, and POST action workflows | `src/views/federation/*.njk`, `src/routes/federation-actions.js` | Started. Connections, conversations, events, groups, listings, member browse, messages, opt-in/out, partner list/detail, settings, and transfer templates now route local links/forms through `urlFor()`. Federation POST action redirects for connection, message, translation, transfer, onboarding, opt-in/out, and settings outcomes now resolve through `res.locals.urlFor`. Focused source and render/action coverage guards the conversion, and a targeted Laravel runtime smoke passed signed federation pages plus body markers for connections, messages, settings, and transfer. Full federation runtime behavior, tenant policy, localization, visual parity, and ASP.NET backend compatibility are not certified. |
| Activity dashboard | `views/activity.blade.php`, `AlphaController::activity()`, `MemberActivityService::getDashboardData()` | `src/routes/activity.js`, `src/views/activity/index.njk` | Partial. GET `/activity` now redirects unsigned visitors to `/login?status=auth-required`, calls the Laravel-compatible profile activity dashboard endpoint, and renders Blade-style summary stats, recent engagement, net balance, skills breakdown, monthly hours progress rows, detailed-insights link, and recent activity timeline. The detailed-insights link now uses `urlFor()`, and route-level auth redirects now use `res.locals.urlFor`, with source-level and shared-mount regressions guarding tenant-aware helper conversion. Exact service row shape, tenant captions, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Activity insights | `views/activity-insights.blade.php`, `ActivityParity::activityInsights()`, `MemberActivityService::getDashboardData()` | `src/routes/activity.js`, `src/views/activity/insights.njk` | Partial. GET `/activity/insights` now redirects unsigned visitors to `/login?status=auth-required`, calls the Laravel-compatible profile activity dashboard endpoint, and renders Blade-style headline stats, dual monthly chart, type-tagged timeline, quick stats card, net-balance tag, and skills card. The back-to-activity links now use `urlFor()`, and route-level auth redirects now use `res.locals.urlFor`, with source-level and shared-mount regressions guarding tenant-aware helper conversion. Exact service row shape, tenant captions, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation partners list | `views/federation-partners.blade.php`, `FederationV2Controller::partners()` | `src/routes/federation.js`, `src/views/federation/partners.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation partners and render Blade-style partner cards with external/level tags, stats, since dates, permission tags, and community links. Tenant policy, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation partner detail | `views/federation-partner.blade.php`, `FederationV2Controller::partnerDetail()` | `src/routes/federation.js`, `src/views/federation/partner.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation partner detail and render Blade-style partner heading, summary fields, permission tags, and browse links scoped by internal partner id. Tenant policy, localization, remote/external partner runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation members list | `views/federation-members.blade.php`, `FederationV2Controller::members()` | `src/routes/federation.js`, `src/views/federation/members.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation members and partners endpoints, preserve query filters, and render Blade-style search, community, skills, service-reach filters, member cards, and cursor load-more links. Operation gating, localization, remote/external partner runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation member detail | `views/federation-member.blade.php`, `FederationV2Controller::member()`, `memberReviews()`, `getSettings()` | `src/routes/federation.js`, `src/views/federation/member.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation member detail, settings, and reviews endpoints and render Blade-style status banners, profile header, reputation, summary fields, connection/message/transfer actions, and review cards. Local member-detail navigation and action targets now use `urlFor()` with source regression coverage. Operation gating, localization, remote/external partner runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation transfer confirmation | `views/federation-transfer.blade.php`, `AlphaController::federationTransfer()`, `FederationV2Controller::member()`, `getSettings()` | `src/routes/federation.js`, `src/views/federation/transfer.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call member detail, federation settings, and wallet balance APIs and render the Blade-style recipient summary, balance, warning text, status errors, CSRF form, amount field, description field, and cancel link. Laravel direct DB recipient resolution, tenant operation gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation settings | `views/federation-settings.blade.php`, `AlphaController::federationSettings()`, `FederationV2Controller::getSettings()` | `src/routes/federation.js`, `src/views/federation/settings.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation settings and render the Blade-style status banner, active notice, visibility checkboxes, notification checkbox, messaging/transaction toggles, service reach select, travel radius field, save form, and opt-out warning link. Tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation opt-in | `views/federation-opt-in.blade.php`, `AlphaController::federationOptIn()`, `FederationV2Controller::getSettings()`, `partners()` | `src/routes/federation.js`, `src/views/federation/opt-in.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests redirect already opted-in members to settings, call Laravel v2 settings and partners, and render the Blade-style benefits cards, privacy/communication/reach preferences, status errors, partner preview, CSRF form, and do-this-later link. Tenant feature gates, exact tenant caption, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation opt-out | `views/federation-opt-out.blade.php`, `AlphaController::federationOptOut()` | `src/routes/federation.js`, `src/views/federation/opt-out.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests render the Blade-style warning text, CSRF opt-out form, warning button, and cancel link to settings. Tenant feature gates, exact tenant caption, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation onboarding | `views/federation-onboarding.blade.php`, `FederationParity::federationOnboarding()`, `FederationV2Controller::getSettings()`, `partners()` | `src/routes/federation.js`, `src/views/federation/onboarding.njk`, `src/routes/federation-actions.js` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests redirect opted-in members to the federation hub, call Laravel v2 settings, render the Blade-style welcome/privacy/communication/confirm wizard, preserve default sharing choices, show status errors, preview internal partner communities on confirm, and post to the existing Laravel-compatible `/federation/onboarding` action alias. The wizard back link, service navigation, step form actions, step-back links, and do-this-later links route through `urlFor()`. Privacy/communication choices now persist in a tenant-keyed Express session bag; confirm submits only its step, retains state on failure, clears it only after successful `/setup`, and remains isolated across tenant mounts. Six focused multi-request tests pass, and a current-source live flow read every choice back from Laravel before restoring the E2E fixture. Tenant feature gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation listings | `views/federation-listings.blade.php`, `AlphaController::federationListings()`, `FederationV2Controller::listings()`, `partners()` | `src/routes/federation.js`, `src/views/federation/listings.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation listings and partners, preserve search/type/community/cursor filters, filter partner options to internal communities, and render Blade-style listing filters, offer/request cards, category/location/estimated-hours/posted-by/community metadata, empty state, and load-more link. Tenant operation gates, external partner listing depth, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation listing detail | `views/federation-listing-show.blade.php`, `AlphaController::federationListingShow()`, `FederationV2Controller::listings()`, `members()`, `getSettings()` | `src/routes/federation.js`, `src/views/federation/listing-show.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests derive the authorized listing from Laravel v2 federation listings filtered to the partner community, render Blade-style tags, image, summary list, full description, profile link, and contact button when member/settings APIs prove messaging is enabled. Exact SQL-level detail lookup, partner messaging gate parity, external partner details, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Listing index and form | `views/listings.blade.php`, `views/listing-create.blade.php`, `views/listing-edit.blade.php`, `AlphaController::listings()`, `AlphaController::createListing()`, `AlphaController::editListing()` | `src/routes/listings.js`, `src/views/listings/index.njk`, `src/views/listings/form.njk` | Partial. Signed `/listings` renders the local protected listing index and owner controls; create/edit forms render local listing fields. Listing index/form breadcrumbs, browse filters, clear/create CTAs, row detail/edit/delete controls, pagination, empty-state CTAs, create/edit form action, and cancel link now use `urlFor()` with source regression coverage, focused render tests, and focused Laravel runtime smoke for signed `/listings`. Exact Blade form field depth, generated value repopulation, image and skill-tag form parity, tenant gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Listing report form | `views/listing-report.blade.php`, `AlphaController::listingReport()`, `Api\ListingsController::report()` | `src/routes/listings.js`, `src/views/listings/report.njk`, `src/lib/api.js` | Partial. Signed GET `/listings/{id}/report` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/listings/{id}`, blocks owner self-reports, and renders the Blade-style back link, listing caption, error summary, reason radios, details textarea, warning submit, and cancel link. POST `/listings/{id}/report` already submits to Laravel `/api/v2/listings/{id}/report`. Listing report entry points in the shared report-link partial now use `urlFor()` with source regression coverage. Tenant module gates, duplicate-report display depth, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Listing exchange request form | `views/exchange-request.blade.php`, `AlphaController::requestExchange()`, `AlphaController::storeExchangeRequest()` | `src/routes/listings.js`, `src/views/listings/exchange-request.njk`, `src/lib/api.js` | Partial. Signed GET `/listings/{id}/exchange-request` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible listing and wallet balance endpoints, blocks owner self-requests, and renders the Blade-style back link, status error summary, listing summary, balance inset, low-balance warning, proposed-hours/prep-time/message fields, and send button. POST `/listings/{id}/exchange-request` already creates exchanges through the Laravel-compatible exchange request API. Exchange workflow feature-disable detection, compliance details, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Listing owner analytics | `views/listings-analytics.blade.php`, `ListingsParity::listingsAnalytics()`, `Api\ListingsController::analytics()` | `src/routes/listings.js`, `src/views/listings/analytics.njk`, `src/lib/api.js` | Partial. Signed GET `/listings/{id}/analytics` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible listing and analytics endpoints, preserves Laravel's 7/14/30/60/90 day selector, and renders the Blade-style listing links, key metrics, trend, created/expires dates, views/contact series, and contact-type table. Tenant module gates, exact owner/admin authorization display depth, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Listing comments | `views/listings-comments.blade.php`, `partials/listings-comment.blade.php`, `ListingsParity::listingsComments()`, `ListingsParity::listingsStoreComment()` | `src/routes/listings.js`, `src/views/listings/comments.njk`, `src/views/listings/_comment.njk`, `src/lib/api.js` | Partial. Signed GET `/listings/{id}/comments` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible listing and comments endpoints, renders the Blade-style back link, status banners/error summary, recursive comments, count, add-comment form, and invalid-comment error state. POST `/listings/{id}/comments` already submits listing comments through Laravel-compatible `/api/v2/comments`. Tenant module gates, comment authorization depth, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation groups | `views/federation-groups.blade.php`, `AlphaController::federationGroups()`, `FederationV2Controller::groups()`, `partners()` | `src/routes/federation.js`, `src/views/federation/groups.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation groups and partners, preserve search/community/cursor filters, filter partner options to internal communities, and render Blade-style search, community filter, group cards, private tags, community/member metadata, empty state, and load-more link. Tenant operation gates, external partner groups, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Groups index and forms | `accessible-frontend::groups`, `accessible-frontend::group-create`, `accessible-frontend::group-edit`, `AlphaController::groups()`, `AlphaController::createGroup()`, `AlphaController::editGroup()` | `src/routes/groups.js`, `src/views/groups/index.njk`, `src/views/groups/new.njk`, `src/views/groups/edit.njk`, `src/views/groups/my.njk`, `src/lib/api.js` | Partial. Group list, create form, edit form, and legacy my-groups source templates are Laravel-backed candidates using Laravel-compatible group APIs and protected local routes. Group list create CTA, search/clear form, group card links, pagination base URL, create/edit form actions, breadcrumbs, back links, cancel links, and legacy my-groups links now use `urlFor()` with source regression coverage. Exact Blade list/form markup, owner policy branches, validation replay depth, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation events | `views/federation-events.blade.php`, `AlphaController::federationEvents()`, `FederationV2Controller::events()`, `partners()` | `src/routes/federation.js`, `src/views/federation/events.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation events and partners, preserve search/community/upcoming/cursor filters, filter partner options to internal communities, and render Blade-style event filters, upcoming toggle, event cards, online/past tags, organiser/community/location/attendee metadata, empty states, and load-more link. Tenant operation gates, external partner events, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Event category browse | `views/events-browse.blade.php`, `EventsParity::eventsBrowse()` | `src/routes/events.js`, `src/views/events/browse.njk`, `src/lib/api.js` | Partial. GET `/events/browse` calls the Laravel-compatible category API, renders the Blade-style back link, caption, category radio chooser, all-events option, selected category state, and view-all link. Browse back/view-all links and the filter form now use `urlFor()` with source regression, mounted `/acme/accessible/events/browse` render coverage, and scoped Laravel runtime smoke for the Blade heading. Tenant feature gates, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Event poll attachment | `views/events-polls.blade.php`, `EventsParity::eventsPolls()` | `src/routes/events.js`, `src/views/events/polls.njk`, `src/lib/api.js` | Partial. Signed GET `/events/{id}/polls` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible event detail and `mine` poll APIs, and renders the Blade-style back link, caption, success/error states, organiser poll checkbox list, attached tags, empty state, and `poll_ids[]` save form. Poll back link and save form now use `urlFor()`, poll save/vote outcome redirects route through `res.locals.urlFor`, and scoped Laravel runtime smoke covers `/events/6/polls`. Exact owner authorization depth, tenant feature gates, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Event translation chooser | `views/events-translate.blade.php`, `EventsParity::eventsTranslate()` | `src/routes/events.js`, `src/views/events/translate.njk`, `src/lib/api.js` | Partial. Signed GET `/events/{id}/translate` redirects unsigned visitors to `/login?status=auth-required`, calls the Laravel-compatible event detail API, and renders the Blade-style back link, caption, status insets/errors, 11-language selector, source description, and no-JS translate form. Translation back link/form now use `urlFor()`, and translation empty, success, failure, and auth redirects route through `res.locals.urlFor`; focused shared-mount coverage proves signed POST and GET controls stay under `/{tenantSlug}/accessible`, and scoped Laravel runtime smoke covers `/events/6/translate`. Rendering translated output after POST, tenant feature gates, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Event recurring edit scope | `views/events-recurring-edit.blade.php`, `EventsParity::eventsRecurringEdit()` | `src/routes/events.js`, `src/views/events/recurring-edit.njk`, `src/lib/api.js` | Partial. Signed GET `/events/{id}/recurring-edit` redirects unsigned visitors to `/login?status=auth-required`, redirects non-series events to `/events/{id}/edit`, calls the Laravel-compatible event detail API, and renders the Blade-style repeating-event form with details, datetime-local fields, single/all scope radios, warning text, and occurrence links. Back link, edit form, occurrence links, auth, non-series, success, and failure redirects now use `urlFor()`/`res.locals.urlFor` for tenant-aware mounted paths. Exact owner authorization depth, validation error replay, tenant feature gates, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation connections | `views/federation-connections.blade.php`, `AlphaController::federationConnections()`, `FederationV2Controller::connections()` | `src/routes/federation.js`, `src/views/federation/connections.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests map Laravel tab filters to `/api/v2/federation/connections`, render Blade-style full-page connection tabs, status banners, empty states, connection cards, incoming request messages, and accept/reject/remove forms. Tenant operation gates, load-error rendering, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation messages index | `views/federation-messages.blade.php`, `AlphaController::federationMessages()`, `FederationV2Controller::messages()`, `getSettings()` | `src/routes/federation.js`, `src/views/federation/messages.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests call Laravel v2 federation settings and messages, group message rows into Blade-style conversation cards, preserve query filtering, and render status banners, opt-in messaging notice, search, compose link, unread tags, previews, and conversation links. Tenant operation gates, load-error rendering, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Federation conversation detail | `views/federation-conversation.blade.php`, `AlphaController::federationConversation()`, `FederationV2Controller::messages()`, `markMessagesReadBatch()` | `src/routes/federation.js`, `src/views/federation/conversation.njk` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in requests filter Laravel v2 federation messages into the selected partner conversation, mark unread inbound IDs read, and render Blade-style message cards, status banners, translate forms, and the reply form. Exact DB read semantics, translation flash display, tenant operation gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Message conversations | `views/conversation.blade.php`, `views/messages-groups.blade.php`, `views/messages-group-create.blade.php`, `views/messages-group-conversation.blade.php`, `AlphaController::conversation()`, `MessagesParity`, `GroupConversationController`, `Api\MessagesController`, `Api\GroupConversationController` | `src/routes/messages.js`, `src/views/messages/index.njk`, `conversation.njk`, `direct-conversation.njk`, `groups.njk`, `group-create.njk`, `group-conversation.njk`, `src/lib/api.js` | Partial. Signed `/messages/new/{userId}` calls Laravel-compatible direct-message APIs, marks the direct conversation read, loads optional listing context, and renders the Blade-style conversation title, listing inset, status/error banners, search form, older-message link, message cards, reply form, voice upload, and archive action. Signed `/messages/groups` calls Laravel-compatible group conversation APIs and renders the Blade-style group list, direct/groups subnav, status banners, unread tags, latest-message previews, and create link. `/messages/groups/new` renders the no-JS search/picker/create form through Laravel user search. `/messages/groups/{id}` reads Laravel messages and participants, renders member badges, query filtering, reaction forms, reply form, admin member controls, and leave form. Message index/direct/group templates now route breadcrumbs, conversation links, listing/member/connection links, pagination, search forms, reply/edit/delete/voice/archive forms, group tabs, create/search/member/reaction forms, and leave controls through `urlFor()` with source regression coverage. Exact restriction/feature gates, translated-text display, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group invite members | `views/groups-invite.blade.php`, `GroupsParity::groupsInvite()`, `GroupInviteService` | `src/routes/groups.js`, `src/views/groups/invite.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/invite` now reads Laravel-compatible group detail and invite listing data, then renders the Blade-style back link, caption, generated invite-link inset, expiry form, email invite form, pending invitations table, revoke forms, and status banners. POST aliases already call Laravel-compatible invite link, email, and revoke endpoints. Owner/admin authorization depth, tenant feature gates, localization, live invite-link flash behavior, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group notification preferences | `views/groups-notifications.blade.php`, `GroupsParity::groupsNotificationPrefs()`, `GroupNotificationPreferenceService` | `src/routes/groups.js`, `src/views/groups/notifications.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/notifications` now reads Laravel-compatible group detail and notification preference data, then renders the Blade-style back link, caption, success/error states, frequency radios, channel checkboxes, and save form. The existing POST alias already calls the Laravel-compatible notification preference endpoint. Active-member/admin authorization depth, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group image management | `views/groups-image.blade.php`, `GroupsParity::groupsImage()`, `GroupsParity::groupsUpdateImage()` | `src/routes/groups.js`, `src/views/groups/image.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/image` now reads Laravel-compatible group detail and renders the Blade-style back link, caption, current avatar/cover previews or empty insets, multipart avatar/cover upload forms, and status banners. The existing POST alias already proxies avatar/cover uploads to the Laravel-compatible group image endpoint. Owner/admin authorization depth, upload validation parity, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group announcements | `views/group-announcements.blade.php`, `views/group-announcements-edit.blade.php`, `GroupsParity::groupsAnnouncements()`, `GroupsParity::groupsEditAnnouncement()`, `GroupAnnouncementService` | `src/routes/groups.js`, `src/views/groups/announcements.njk`, `src/views/groups/announcement-edit.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/announcements` now reads Laravel-compatible group detail and announcement listing data, then renders the Blade-style back link, caption, success/error states, announcement summary cards, pinned/expired tags, admin edit/pin/delete controls, empty state, and create form. The announcements source template routes its back link, edit link, pin/delete forms, and create form through `urlFor()` with source regression coverage. Signed GET `/groups/{id}/announcements/{annId}/edit` reads Laravel-compatible group and announcement detail data, then renders the Blade-style edit form and validation states. Existing announcement POST aliases already call Laravel-compatible create/update/delete/pin endpoints. Active-member/admin authorization depth, old-input replay, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group discussions | `views/group-discussions.blade.php`, `views/group-discussion-create.blade.php`, `views/group-discussion-detail.blade.php`, `AlphaController::groupDiscussions()`, `AlphaController::createGroupDiscussion()`, `AlphaController::groupDiscussion()`, `GroupService` | `src/routes/groups.js`, `src/views/groups/discussions.njk`, `src/views/groups/discussion-create.njk`, `src/views/groups/discussion-detail.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/discussions` now reads Laravel-compatible group detail and discussion listing data, then renders the Blade-style back link, caption, status states, member-only start button, empty state, discussion cards, pinned tags, and reply counts. Signed GET `/groups/{id}/discussions/new` renders the Blade-style start-discussion form. Signed GET `/groups/{id}/discussions/{discussionId}` reads Laravel-compatible thread data and renders the Blade-style discussion article, reply list, status states, and reply form. Existing discussion POST aliases already call Laravel-compatible create/reply endpoints. Active-member authorization depth, old-input validation replay, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group files | `views/group-files.blade.php`, `GroupsParity::groupsFiles()`, `GroupsParity::groupsDownloadFile()`, `GroupFileService` | `src/routes/groups.js`, `src/views/groups/files.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/files` now reads Laravel-compatible group detail and file listing data, then renders the Blade-style back link, caption, status banners, empty state, file table, download/delete controls, and multipart upload form. The files source template routes its back link, download link, delete form, and upload form through `urlFor()` with source regression coverage. Signed GET `/groups/{id}/files/{fileId}/download` proxies the Laravel-compatible binary download response and preserves content type, disposition, length, cache, etag, and last-modified headers. Existing upload/delete POST aliases already call Laravel-compatible group file endpoints. Active-member/admin authorization depth, uploader-only delete visibility, upload validation parity, tenant feature gates, localization, runtime behavior against a live Laravel filesystem, and ASP.NET backend compatibility are not certified. |
| Group management | `views/group-manage.blade.php`, `AlphaController::manageGroup()`, `GroupService` | `src/routes/groups.js`, `src/views/groups/manage.njk`, `src/lib/api.js` | Partial. Signed GET `/groups/{id}/manage` now reads Laravel-compatible group detail, `/api/v2/groups/{id}/members?limit=100`, and `/api/v2/groups/{id}/requests`, then renders the Blade-style back link, caption, success/error states, pending join-request cards, approve/reject forms, member cards, owner/admin/member tags, promote/demote controls, and remove controls. Existing member and request POST aliases already call Laravel-compatible group member/request endpoints. Owner/admin authorization depth, current-user filtering parity, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Events index and forms | `views/events.blade.php`, `views/event-form.blade.php`, `AlphaController::events()`, `AlphaController::createEvent()`, `AlphaController::editEvent()` | `src/routes/events.js`, `src/views/events/index.njk`, `src/views/events/new.njk`, `src/views/events/edit.njk`, `src/lib/api.js` | Partial. The event list, create form, and edit form are Laravel-backed candidates with Laravel-compatible category loading, recurring event creation, online/remote attendance fields, and cover image upload/edit support. Event list create CTA, search form, event and group links, pagination, empty-state actions, create/edit form actions, breadcrumbs, back links, and cancel links now use `urlFor()` with source regression coverage. Exact Blade list markup, all organizer policy branches, cover image removal, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Event location map | `views/events-map.blade.php`, `EventsParity::eventsMap()` | `src/routes/events.js`, `src/views/events/map.njk`, `src/lib/api.js` | Partial. GET `/events/{id}/map` calls Laravel `/api/v2/events/{id}` through the event API helper and renders the Blade-style back link, caption, location summary list, OpenStreetMap iframe and outbound links, and online/no-coordinate empty states. The local event back link now uses `urlFor()` with source regression, mounted `/acme/accessible/events/{id}/map` render coverage, and scoped Laravel runtime smoke for `/events/6/map`, while external OpenStreetMap URLs remain unchanged. Maps/events feature gates, localization, full runtime behavior, and ASP.NET backend compatibility are not certified. |
| Blog GET pages | `views/blog-index.blade.php`, `views/blog-post.blade.php`, `views/blogreviews-post-comments.blade.php`, `views/blogreviews-likers.blade.php`, `AlphaController::blog()`, `blogPost()`, `BlogReviewsParity::blogReviewsPostComments()`, `blogReviewsPostLikers()` | `src/routes/blog-posts.js`, `src/views/blog/index.njk`, `src/views/blog/detail.njk`, `src/views/blog/comments.njk`, `src/views/blog/likers.njk`, `src/lib/api.js` | Partial. `/blog`, `/blog/{slug}`, signed `/blog/{slug}/comments`, signed `/blog/{slug}/likers/{reaction}`, and `/blog/feed.xml` now call Laravel-compatible blog, comment, and reaction APIs and render Blade-style listing, detail, discussion, reaction, and liker pages. Blog links/forms and route-level auth/result redirects now use `urlFor()`/`res.locals.urlFor` so shared tenant mounts and custom-domain child paths do not leak flat `/blog`, `/reviews`, or `/login` targets. Feature gates, tenant captions, exact Laravel rich text policy, localization, RSS metadata depth, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Poll GET pages | `views/polls.blade.php`, `views/gamification-poll-detail.blade.php`, `views/gamification-poll-rank.blade.php`, `views/gamification-poll-create.blade.php`, `views/gamification-poll-manage.blade.php`, `AlphaController::polls()`, `GamificationParity::gamificationPollDetail()`, `gamificationRankedVote()`, `gamificationCreatePoll()`, `gamificationManagePolls()`, `gamificationExportPoll()` | `src/routes/poll-actions.js`, `src/routes/polls.js`, `src/views/polls/index.njk`, `detail.njk`, `rank.njk`, `create.njk`, `manage.njk`, `src/lib/api.js` | Partial. Signed `/polls`, `/polls/{id}`, `/polls/{id}/rank`, `/polls/parity/create`, `/polls/parity/manage`, and `/polls/{id}/export` now call Laravel-compatible poll, ranked-results, category, comment, like, and export APIs and render Blade-style poll list, detail, ranked result/vote, create, manage, and CSV flows. Poll action redirects and the legacy poll vote success/API-error redirects now use `res.locals.urlFor` for tenant-mounted and custom-domain contexts. Feature gates, exact service-level open/closed list parity, owner authorization depth, tenant captions, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Goals index GET page | `views/goals.blade.php`, `AlphaController::goals()`, `Api\GoalsController::index()` | `src/routes/goals.js`, `src/views/goals/index.njk`, `src/lib/api.js` | Partial. Signed `/goals` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/goals?per_page=30`, and renders Blade-style status banners, module links, goal cards, progress bars, empty state, and create-goal form. Detail/depth GET pages, exact tenant captions, goals feature gates, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Clubs GET page | `views/clubs.blade.php`, `AlphaController::clubs()`, `Api\ClubsApiController::index()` | `src/routes/clubs.js`, `src/views/clubs/index.njk`, `src/lib/api.js` | Partial. Signed `/clubs` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/clubs` with the search query, and renders Blade-style club search, empty state, logo, member count, schedule, contact, and external website link cards. The clubs search form now routes through `urlFor('/clubs')`, and the unsigned redirect uses `res.locals.urlFor` so shared tenant mounts and custom-domain child paths do not leak flat `/clubs` or `/login` targets. The route mirrors Laravel's active-club gate by returning `404` when an unfiltered Laravel clubs response proves the tenant has no active club evidence, while searched empty results can render when a minimal unfiltered probe proves active clubs exist. Exact tenant caption, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Skills GET page | `views/skills.blade.php`, `AlphaController::skills()`, `Api\SkillTaxonomyController` | `src/routes/skills.js`, `src/views/skills/index.njk`, `src/lib/api.js` | Partial. Signed `/skills` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/skills/categories`, `/api/v2/skills/categories/{id}`, and `/api/v2/skills/members`, and renders Blade-style skill search, member results with proficiency/offers/wants tags, category skill counts, and nested category browsing. The skills search form, category links, back link, skill-search links, member profile links, unsigned redirect, and Laravel-401 redirect helper path now route through `urlFor()`/`res.locals.urlFor` so shared tenant mounts and custom-domain child paths do not leak flat `/skills`, `/members`, or `/login` targets. Exact tenant captions, localization, category/member authorization edge cases, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Ideation challenge list/create/edit/manage/outcome-edit/drafts/detail/idea-detail/tags/campaigns/outcomes | `views/ideation.blade.php`, `views/ideation-challenge-form.blade.php`, `views/ideation-manage.blade.php`, `views/ideation-outcome-form.blade.php`, `views/ideation-drafts.blade.php`, `views/ideation-detail.blade.php`, `views/ideation-idea.blade.php`, `views/ideation-tags.blade.php`, `views/ideation-campaigns.blade.php`, `views/ideation-campaign-detail.blade.php`, `views/ideation-outcomes.blade.php`, `partials/ideation-nav.blade.php`, `AlphaController::ideation()`, `IdeationParity::ideationCreateChallenge()`, `IdeationParity::ideationEditChallenge()`, `IdeationParity::ideationManageChallenge()`, `IdeationParity::ideationOutcomeEdit()`, `IdeationParity::ideationDrafts()`, `IdeationParity::ideationIdeaDetail()`, `AlphaController::ideationChallenge()`, `IdeationParity::ideationPopularTags()`, `IdeationParity::ideationCampaigns()`, `IdeationParity::ideationCampaignDetail()`, `IdeationParity::ideationOutcomes()`, `IdeationChallengeService`, `ChallengeCategoryService`, `ChallengeTemplateService`, `CampaignService`, `ChallengeOutcomeService`, `IdeaMediaService` | `src/routes/ideation.js`, `src/views/ideation/index.njk`, `src/views/ideation/challenge-form.njk`, `src/views/ideation/manage.njk`, `src/views/ideation/outcome-form.njk`, `src/views/ideation/drafts.njk`, `src/views/ideation/detail.njk`, `src/views/ideation/idea-detail.njk`, `src/views/ideation/tags.njk`, `src/views/ideation/campaigns.njk`, `src/views/ideation/campaign-detail.njk`, `src/views/ideation/outcomes.njk`, `src/views/ideation/_nav.njk`, `src/lib/api.js` | Partial. Signed `/ideation`, `/ideation/new`, `/ideation/{id}`, `/ideation/{id}/edit`, `/ideation/{id}/manage`, `/ideation/{id}/outcome`, `/ideation/{id}/drafts`, `/ideation/{id}/ideas/{ideaId}`, `/ideation/tags`, `/ideation/campaigns`, `/ideation/campaigns/{id}`, and `/ideation/outcomes` now redirect unsigned users to `/login?status=auth-required`, call Laravel-compatible `/api/v2/ideation-challenges`, `/api/v2/ideation-categories`, `/api/v2/ideation-templates`, `/api/v2/ideation-challenges/{id}`, `/api/v2/ideation-challenges/{id}/ideas`, `/api/v2/ideation-challenges/{id}/ideas/drafts`, `/api/v2/ideation-challenges/{id}/outcome`, `/api/v2/ideation-ideas/{ideaId}`, `/api/v2/ideation-ideas/{ideaId}/comments`, `/api/v2/ideation-ideas/{ideaId}/media`, `/api/v2/ideation-tags/popular`, `/api/v2/ideation-campaigns`, `/api/v2/ideation-campaigns/{id}`, and `/api/v2/ideation-outcomes/dashboard`, and render Blade-style challenge filters/cards, create/edit form fields, category/template choices, manage lifecycle/campaign/favourite/duplicate/delete controls, outcome winning-idea/status/impact fields, draft edit/publish forms, detail metadata, prize inset, idea cards/detail, voting forms, submit-idea form, comment/media/admin/convert/delete idea controls, popular tag links, selected-tag challenge matches, tag empty states, ideation tabs, campaign status banners, campaign cards/detail metadata, linked challenge cards, challenge counts, creator metadata, outcome stats, and outcome tables. Ideation source templates now route local links/forms through `urlFor()`, and ideation action redirects now use `res.locals.urlFor`, with shared-mount coverage for challenge create redirects under `/{tenantSlug}/accessible`. Admin authorization depth, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Resources simple GET page | `views/resources.blade.php`, `AlphaController::resources()`, `Api\ResourcePublicController::index()` | `src/routes/resources.js`, `src/views/resources/index.njk`, `src/lib/api.js` | Partial. Signed `/resources` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/resources` with the search query, and renders Blade-style simple resource search, empty state, type tags, descriptions, full-library link, and download links. Feature gates, exact service-level list semantics, tenant captions, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Resources library GET page | `views/resources-library.blade.php`, `partials/resources-category-tree.blade.php`, `ResourcesParity::resourcesLibrary()`, `Api\ResourcePublicController::index()`, `Api\ResourcePublicController::categories()`, `Api\ResourceCategoryController::tree()` | `src/routes/resources.js`, `src/views/resources/library.njk`, `src/lib/api.js` | Partial. Signed `/resources/library` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/resources`, `/api/v2/resources/categories`, `/api/v2/resources/categories/tree`, and profile data, and renders Blade-style library search, category sidebar, status banners, resource count, rich resource cards, metadata, download/comment/reaction links, load-more links, admin-only reorder toggle links, and admin-only Move up/Move down forms that post to the Laravel-compatible reorder API. Exact role ownership display, tenant captions, feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Resources comments GET page | `views/resources-comments.blade.php`, `ResourcesParity::resourcesComments()`, `CommentService`, `ReactionService` | `src/routes/resources.js`, `src/views/resources/comments.njk`, `src/lib/api.js` | Partial. Signed `/resources/{id}/comments` now redirects unsigned users to `/login?status=auth-required`, loads the resource title through Laravel-compatible `/api/v2/resources`, loads thread data through `/api/v2/comments?target_type=resource&target_id=...`, loads reactions through `/api/v2/reactions/resource/{id}`, and renders Blade-style discussion, status banners, reaction controls, threaded comments, owner delete buttons, and add-comment form. Exact resource lookup depth, owner/admin authorization rendering, tenant feature gates, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Resources upload GET/POST page | `views/resources-upload.blade.php`, `ResourcesParity::resourcesUploadForm()`, `ResourcesParity::resourcesUpload()`, `ResourcesParity::resourcesFlatCategories()` | `src/routes/resources.js`, `src/views/resources/upload.njk`, `src/lib/api.js`, `src/middleware/multipart.js` | Partial. Signed `/resources/upload` now redirects unsigned users to `/login?status=auth-required`, loads category options through Laravel-compatible `/api/v2/resources/categories`, renders Blade-style back link, caption, upload guidance, failure error summary, multipart form, title, description, category, file controls, allowed file-type hint, submit, and cancel link, and proxies multipart submissions to Laravel `/api/v2/resources` before redirecting to `/resources/library?status=resource-uploaded`. Tenant feature gates, category fallback exactness, validation error replay, localization, live upload smoke behavior, and ASP.NET backend compatibility are not certified. |
| Resources delete GET page | `views/resources-delete.blade.php`, `ResourcesParity::resourcesDeleteConfirm()` | `src/routes/resources.js`, `src/views/resources/delete.njk`, `src/lib/api.js` | Partial. Signed `/resources/{id}/delete` now redirects unsigned users to `/login?status=auth-required`, locates the resource through Laravel-compatible `/api/v2/resources`, checks collection owner/admin metadata plus profile ownership where available, and renders Blade-style back link, caption, irreversible warning, confirmation question, CSRF delete form, warning button, and cancel link. Exact DB-backed lookup, full admin authorization parity, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Resources download GET route | `ResourcesParity::resourcesDownload()`, `Api\ResourcePublicController::download()` | `src/routes/resources.js`, `src/lib/api.js` | Partial. Signed `/resources/{id}/download` now redirects unsigned users to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/resources/{id}/download`, proxies the binary body, and preserves content type, disposition, length, cache, etag, and last-modified headers. Tenant feature gates, runtime file streaming against a live Laravel backend, ASP.NET backend compatibility, and exact Laravel filesystem/error behavior are not certified. |
| Account hub | `views/account.blade.php` | `src/views/account.njk` | Partial. Unsigned requests redirect to `/login`; signed-in users see the Blade-style card list for wallet, messages, connections, notifications, profile, settings, and a CSRF-protected sign-out form. Account card links and the logout form action now use `urlFor()`, with a source-level regression guarding the tenant-aware helper conversion. The default Laravel runtime smoke now verifies POST `/logout` redirects to `/login` and clears the signed account session. Notification group-read/delete-all, wallet donate, saved-item removal, saved-collection CRUD/item-remove, match-dismiss, and appreciation send/react POST aliases are backed by Laravel v2 endpoints. Tenant feature gating, full account-link coverage, per-module data, realtime behavior, route availability checks, and ASP.NET backend compatibility are not certified. |
| Notifications inbox | `views/notifications.blade.php`, `AlphaController::notifications()`, `markAllNotificationsRead()`, `markNotificationRead()`, `deleteNotification()`, `deleteAllNotifications()`, `markGroupNotificationsRead()` | `src/routes/notifications.js`, `src/views/notifications/index.njk`, `src/lib/api.js` | Partial. Signed `/notifications` redirects unsigned visitors to `/login?status=auth-required`, reads Laravel-compatible notification payloads, and renders Blade-style inbox filters, unread status, notification cards, per-notification read/delete actions, and empty states. Notification breadcrumbs, filters, read/delete actions, redirect hidden values, pagination, and empty-state CTA now use `urlFor()` with source regression coverage. Group-read/delete-all handlers are covered by existing route tests, and the focused notifications render test remains green. Exact Laravel grouping display, module gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Profile delete account | `views/profile-delete.blade.php`, `AlphaController::confirmDeleteAccount()` | `src/routes/profile.js`, `src/views/profile/delete.njk` | Partial. GET `/profile/delete-account` now redirects unsigned visitors to `/login?status=auth-required` and renders the Blade-style back link, warning text, irreversible-effects copy, password field, optional reason, confirmation checkbox, warning button, cancel link, and status error summary. The existing POST alias keeps Laravel delete status redirects and calls the Laravel-compatible user-settings delete endpoint. Delete-account back/cancel links and form action now use `urlFor()`, with source-level regression coverage. Tenant-domain routing, localization, live password verification behavior, and ASP.NET backend compatibility are not certified. |
| Profile settings | `views/profile-settings.blade.php`, `AlphaController::profileSettings()` | `src/routes/profile.js`, `src/views/profile/settings.njk` | Partial. GET `/profile/settings` now redirects unsigned visitors to `/login?status=auth-required`, loads Laravel-compatible profile/account/notification/match/skill/passkey/session/safeguarding payloads when available, and renders Blade-style settings links, profile/photo/privacy/newsletter form, skills, security/email/password/passkeys/sessions, language, notifications, match, personalisation, safeguarding, and data/privacy sections. Settings card links, profile/security/privacy links, and form actions now use `urlFor()`, with source-level regression coverage. Avatar upload persistence, exact DB-backed helper data, tenant feature gating, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Profile two-step verification | `views/two-factor-setup.blade.php`, `AlphaController::twoFactorSetup()` | `src/routes/profile.js`, `src/views/profile/two-factor.njk` | Partial. GET `/profile/two-factor` now redirects unsigned visitors to `/login?status=auth-required`, reads a Laravel-compatible `/auth/2fa/setup` profile payload when available, and renders the Blade-style setup QR/manual key instructions, verification form, enabled-state backup-code count, disable form, success banner, and status error summary. Back/finish links and verify/disable form actions now use `urlFor()`, with source-level regression coverage. Exact TotpService setup payload generation, one-time backup-code POST rendering, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Profile blocked members | `views/blocked-users.blade.php`, `AlphaController::blockedUsers()` | `src/routes/profile.js`, `src/views/profile/blocked.njk` | Partial. GET `/profile/blocked` now redirects unsigned visitors to `/login?status=auth-required`, reads a Laravel-compatible `/users/blocked` profile payload when available, and renders the Blade-style back link, success banner, blocked member cards, avatar fallback, empty state, and unblock forms that post through the existing Laravel-compatible member unblock alias. Back link and dynamic unblock form actions now use `urlFor()`, with source-level regression coverage. Exact BlockUserService row shape, tenant-domain routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Settings appearance | `views/settings-appearance.blade.php`, `SettingsAuthParity::settingsAppearance()` | `src/routes/settings.js`, `src/views/settings/appearance.njk` | Partial. GET `/settings/appearance` now redirects unsigned visitors to `/login?status=auth-required`, reads `preferred_theme`/`theme` from the Laravel-compatible `/api/v2/users/me` payload when present, falls back to `system`, and renders the Blade-style back link, caption, success/error states, theme radios, hints, and save button. Back link, save form action, auth handoff, validation redirect, success redirect, and API-failure redirect now use `urlFor()`/`res.locals.urlFor`, with source-level regression coverage. Exact Laravel DB-backed theme lookup, tenant-domain routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Settings availability | `views/settings-availability.blade.php`, `SettingsAuthParity::settingsAvailability()` | `src/routes/settings.js`, `src/views/settings/availability.njk` | Partial. GET `/settings/availability` now redirects unsigned visitors to `/login?status=auth-required`, loads the Laravel-compatible `/api/v2/users/me/availability` payload when present, and renders the Blade-style account back link, caption, success/error states, weekly seven-day grid, three time slots per day, time inputs, and save button. Back link, save form action, auth handoff, validation redirect, success redirect, and API-failure redirect now use `urlFor()`/`res.locals.urlFor`, with source-level regression coverage. Exact Laravel DB-backed availability lookup, tenant-domain routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Settings linked accounts | `views/settings-linked-accounts.blade.php`, `SettingsAuthParity::settingsLinkedAccounts()` | `src/routes/settings.js`, `src/views/settings/linked-accounts.njk` | Partial. GET `/settings/linked-accounts` now redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/users/me/sub-accounts` and `/api/v2/users/me/parent-accounts` when available, and renders the Blade-style back link, caption, success/error states, incoming managers, managed accounts, empty states, link request form, relationship choices, permission checkboxes, and action forms. Back link plus approve, permissions, revoke, request form actions, auth handoffs, validation redirects, success redirects, and API-failure redirects now use `urlFor()`/`res.locals.urlFor`, with source-level regression coverage. Exact SubAccountService row shapes, tenant-domain routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Settings data rights | `views/settings-data-rights.blade.php`, `SettingsAuthParity::settingsDataRights()` | `src/routes/settings.js`, `src/views/settings/data-rights.njk` | Partial. GET `/settings/data-rights` now redirects unsigned visitors to `/login?status=auth-required` and renders the Blade-style back link, caption, GDPR status banners/error summary, request radio group, notes field, submit button, and empty request-history state. The existing POST alias submits to Laravel `/api/v2/users/me/gdpr-request` through the user-settings API wrapper. Back link, request form action, auth handoff, validation redirect, success redirect, duplicate redirect, and API-failure redirect now use `urlFor()`/`res.locals.urlFor`, with source-level regression coverage. Request-history loading, tenant-domain routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Settings insurance | `views/settings-insurance.blade.php`, `SettingsAuthParity::settingsInsurance()` | `src/routes/settings.js`, `src/views/settings/insurance.njk` | Partial. GET `/settings/insurance` now redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/users/me/insurance` when available, and renders the Blade-style back link, caption, success/error states, certificate cards, empty state, status tags, and multipart upload form. Back link, upload form action, auth handoff, validation redirects, upload success redirect, and API-failure redirect now use `urlFor()`/`res.locals.urlFor`, with source-level regression coverage. Tenant insurance feature gating, localization, runtime upload/list behavior, and ASP.NET backend compatibility are not certified. |
| Matches index and board | `views/matches.blade.php`, `views/connections-matches-board.blade.php`, `AlphaController::matches()`, `ConnectionsMatchesParity::connectionsMatchesBoard()`, `AlphaController::dismissMatch()`, `ConnectionsMatchesParity::connectionsDismissMatch()` | `src/routes/matches.js`, `src/views/matches/index.njk`, `src/views/matches/board.njk`, `src/lib/api.js` | Partial. Signed `/matches` and `/matches/board` call Laravel-compatible `/api/v2/matches/all`, render the Blade-style summary metrics, source filters, match cards, module/type tags, score tags/progress, match reasons, empty states, and no-JS dismiss forms. POST aliases call Laravel `/api/v2/matches/{id}/dismiss`, preserve Laravel status redirects, and keep the matches-board `source` query plus `#matches-top` fragment. Tenant/module gates, exact API support for event-source filtering, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Exchange list/detail/actions | `views/exchanges.blade.php`, `views/exchange-detail.blade.php`, `AlphaController::exchanges()`, `exchange()`, `storeExchangeAction()`, `storeExchangeRating()` | `src/routes/exchanges.js`, `src/views/exchanges/*.njk` | Partial. Signed `/exchanges` and `/exchanges/{id}` call Laravel-compatible exchange config/list/detail/rating endpoints and render Blade-style tabs, cards, detail summary, role-aware actions, review form, ratings, and timeline. `/exchanges/{id}` maps accept/decline/start/complete/confirm/cancel actions to Laravel `/api/v2/exchanges/{id}` action endpoints, and `/exchanges/{id}/rate` posts ratings to `/api/v2/exchanges/{id}/rate`, preserving Laravel status redirects. Exchange filter tabs, detail links, pagination, listing/message links, action forms, and rating form controls now use `urlFor()` with source regression, focused render coverage, and scoped Laravel runtime smoke for signed `/exchanges`; exchange action/rating POST redirects now use `res.locals.urlFor` with focused shared-mount coverage. Participant authorization, tenant/module gates, exact exchange history/rating display, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Group exchange list/create/detail | `views/group-exchanges.blade.php`, `views/group-exchange-create.blade.php`, `views/group-exchange-detail.blade.php`, `AlphaController::groupExchanges()`, `createGroupExchange()`, `groupExchange()` | `src/routes/group-exchanges.js`, `src/views/group-exchanges/*.njk`, `src/lib/api.js` | Partial. Signed `/group-exchanges`, `/group-exchanges/new`, and `/group-exchanges/{id}` now redirect unsigned users to `/login?status=auth-required`, call Laravel-compatible group exchange and user-search APIs, and render Blade-style status tabs, create form, detail metadata, participant table, confirmation controls, organiser add-person search, complete, and cancel forms. Group-exchange create CTA, status tabs, detail links, create form, participant add/remove/search forms, confirmation form, and complete/cancel actions now use `urlFor()` with source regression coverage. Group-exchange GET auth handoffs and action redirects now use `res.locals.urlFor`, with shared-mount coverage for signed-out list/create/detail redirects and invalid create redirects under `/{tenantSlug}/accessible`. Full organiser/participant authorization depth, same-tenant member search parity, feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Wallet manage and exports | `views/wallet-manage.blade.php`, `AlphaController::exportTransactions()`, `AlphaController::walletRecipients()`, `WalletParity::walletManage()` | `src/routes/wallet.js`, `src/views/wallet/index.njk`, `src/views/wallet/manage.njk`, `src/lib/api.js` | Partial. Signed `/wallet/manage` calls Laravel-compatible wallet balance, community fund, and user-search APIs, then renders the Blade-style back link, balance card, earned/spent/pending stats, transfer recipient search, transfer forms, donation target controls, community-fund summary, and status states. `/wallet/recipients` returns Laravel user-search suggestions for progressive enhancement, and `/wallet/export.csv` streams the Laravel wallet statement CSV with download headers. Wallet index/manage breadcrumbs, manage CTA, back link, recipient search form, transfer forms, and donation forms now use `urlFor()` with source regression coverage. Wallet transfer and donation status redirects also route through `res.locals.urlFor`, with shared-mount coverage for invalid donation validation. Tenant module gates, exact live recipient privacy behavior, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Saved items and collections | `views/saved.blade.php`, `SavedCollectionsParity`, `views/saved-collections.blade.php`, `views/saved-collection-detail.blade.php`, `views/saved-public-collections.blade.php`, `views/saved-appreciations.blade.php` | `src/routes/saved-social.js`, `src/routes/saved-collections.js`, `src/views/saved/index.njk`, `src/views/saved-collections/*.njk`, `src/views/saved-social/*.njk`, `src/lib/api.js` | Partial. Signed `/saved` redirects unsigned users to `/login?status=auth-required`, calls Laravel `/api/v2/bookmarks`, and renders the Blade-style saved item filter, status banner, empty/list states, item links, type tags, and remove forms. Signed `/me/collections`, `/me/collections/{id}`, `/users/{id}/collections`, and `/users/{id}/appreciations` redirect unsigned users to `/login?status=auth-required`; owner pages call Laravel-compatible `/api/v2/me/collections` and `/api/v2/me/collections/{id}/items`, while public pages call `/api/v2/users/{id}/public-collections` and `/api/v2/users/{id}/appreciations`. The pages render Blade-style collection cards, item cards, pagination, create form, edit/delete controls, item-remove forms, thank-you form, appreciation cards, reaction forms, and status messages. POST aliases for saved item removal, create/update/delete, collection item removal, appreciation send, and appreciation reaction call Laravel v2 endpoints with matching status redirects. Route-level saved family redirects now use the active tenant URL helper, including appreciation reaction anchors. Tenant feature gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Help centre and trust safety pages | `views/help.blade.php`, `views/trust-safety.blade.php`, `AlphaController::help()`, `AlphaController::trustSafety()`, `Api\HelpController` | `src/routes/support.js`, `src/views/support/*.njk`, `src/lib/api.js` | Partial. Public `/help` now calls Laravel-compatible `/api/v2/help/faqs` with the search query and renders Blade-style caption, search form, grouped GOV.UK accordions, empty/no-result states, and contact CTA. `/trust-and-safety` now renders the Blade warning text, exchange/safety/vetting/insurance/dispute/responsibility sections, contact CTA, and community-guidelines link. Tenant routing, localization, live FAQ runtime behavior, and ASP.NET backend compatibility are not certified. |
| Legal hub, documents, and accessibility statement | `views/legal-hub.blade.php`, `views/legal-document.blade.php`, `views/accessibility.blade.php`, `AlphaController::legalHub()`, `legalDocument()`, `accessibility()`, `Api\LegalController` | `src/routes/legal.js`, `src/views/legal/*.njk`, `src/lib/api.js` | Partial. Public `/legal` renders the Blade card list, `/accessibility` renders the Blade accessibility statement, and `/legal/terms`, `/legal/privacy`, `/legal/cookies`, `/legal/community-guidelines`, and `/legal/acceptable-use` call Laravel-compatible `/api/v2/legal/{type}` before rendering tenant document title/content/version metadata or the Blade fallback policy sections. Tenant routing, localization, acceptance prompts, version history/compare links, live runtime behavior, and ASP.NET backend compatibility are not certified. |
| Knowledge base index/detail | `views/kb-index.blade.php`, `views/kb-article.blade.php`, `AlphaController::kb()`, `AlphaController::kbArticle()`, `Api\KnowledgeBaseController` | `src/routes/kb.js`, `src/views/kb/*.njk`, `src/lib/api.js` | Partial. Public `/kb` and `/kb/{id}` now call Laravel-compatible `/api/v2/kb`, `/api/v2/kb/search`, and `/api/v2/kb/{id}` and render Blade-style caption, search form, article cards, category/view metadata, cursor load-more link, article back link, author/update metadata, sanitized article body, and related article links. Feedback forms, attachments/downloads, admin editing, tenant routing, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Onboarding wizard | `views/onboarding.blade.php`, `AlphaController::onboarding()`, `onboardingStep()`, `onboardingStepPost()`, `onboardingAvatar()`, `OnboardingController` | `src/routes/onboarding-posts.js`, `src/views/onboarding/index.njk`, `src/lib/api.js` | Partial. Unsigned requests redirect to `/login?status=auth-required`; signed-in `/onboarding` redirects to the first Laravel-configured active step, completed members redirect to the dashboard, and `/onboarding/{step}` renders the Blade-style welcome/profile/interests/skills/safeguarding/confirm wizard from Laravel v2 onboarding status/config/categories/safeguarding APIs plus the profile API. POST aliases store category choices in the Express session, save bio through the profile API, preserve Laravel Blade `safeguarding[id]` fields before Express body parsing compacts them, proxy multipart avatar uploads to Laravel `/api/v2/users/me/avatar`, and submit confirm to Laravel `/api/v2/onboarding/complete`. Onboarding wizard step form actions and confirm-page change links now route through `urlFor()` with source regression coverage for tenant-aware shared mounts and custom-domain child paths. Exact tenant captions, server-side tenant category validation on GET, upload runtime smoke behavior, localization beyond English strings, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Volunteering landing | `views/volunteering.blade.php` | `src/views/volunteering.njk` | Partial. Caption, lead, organisation browse link, how-it-works inset, auth-required notice, filters, opportunity cards, empty/error states, and cursor load-more link are present. Data loads from Laravel `/api/v2/volunteering/opportunities`; applications, recommended shifts, hours, organisation owner tools, feature gates, tenant routing, and POST workflows are not certified. |
| Volunteering opportunity detail | `views/volunteer-opportunity.blade.php` | `src/views/volunteer-opportunity.njk` | Partial. Back link, caption, title, remote tag, description, organisation panel, summary fields, available shifts, auth-required notice, and safe apply link are present. Data loads from Laravel `/api/v2/volunteering/opportunities/{id}`; apply POST, shift signup/cancel, auth redirects, feature gates, tenant routing, and runtime behavior are not certified. |
| Volunteering accessibility needs | `views/volunteering-accessibility.blade.php`, `AlphaController::volunteerAccessibility()` | `src/routes/volunteering-actions.js`, `src/views/volunteering/accessibility.njk`, `src/lib/api.js` | Partial. Signed GET redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/volunteering/accessibility-needs`, and renders the Blade-style back link, success/error status states, need type checkboxes, description, adjustments, emergency contact fields, and no-JS save form. Tenant feature gate behavior, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Volunteering certificates | `views/volunteering-certificates.blade.php`, `AlphaController::volunteeringCertificates()`, `AlphaController::downloadVolunteerCertificate()`, `VolunteerCertificateController::certificateHtml()` | `src/routes/volunteering-actions.js`, `src/views/volunteering/certificates.njk`, `src/lib/api.js` | Partial. Signed GET redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/volunteering/certificates`, and renders the Blade-style back link, success/no-hours status states, generate form, load-error summary, empty state, certificate cards, organisation hour breakdown, verification code, and local download links. Back, generate, and download controls now route through `urlFor()` with source regression coverage. Download first checks the requested code against the signed-in member's `/api/v2/volunteering/certificates` list, then streams Laravel printable HTML from `/api/v2/volunteering/certificates/{code}/html` with the Blade-style inline filename. Tenant feature gate behavior, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Volunteering opportunity create | `views/volunteering-create-opportunity.blade.php`, `VolunteeringParity::volunteeringCreateOpportunity()` | `src/routes/volunteering-actions.js`, `src/views/volunteering/create-opportunity.njk`, `src/lib/api.js` | Partial. Signed GET redirects unsigned visitors to `/login?status=auth-required`, reads `/api/v2/volunteering/my-organisations?per_page=50` and `/api/v2/categories?type=volunteering`, filters to approved/active owner-admin organisations, and renders the Blade-style back link, validation status summary, no-org inset, organisation/category selects, title/description/location/skills/date fields, remote/network checkboxes, and submit button. Exact legacy `volunteer` category coverage, tenant feature gates, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Volunteering credentials | `views/volunteering-credentials.blade.php`, `VolunteeringParity::volunteeringCredentials()`, `VolunteerCertificateController::myCredentials()` | `src/routes/volunteering-actions.js`, `src/views/volunteering/credentials.njk`, `src/lib/api.js` | Partial. Signed GET redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/volunteering/credentials`, and renders the Blade-style back link, upload form, success/error status states, type options, file and expiry fields, credential table, status tags, expiry/uploaded dates, and delete forms. Back, upload, and delete controls now route through `urlFor()` with source regression coverage. Runtime upload/delete persistence, file download, tenant feature gate behavior, localization, and ASP.NET backend compatibility are not certified. |
| Volunteering hours | `views/volunteering-hours.blade.php`, `AlphaController::volunteeringHours()`, `VolunteerController::myHours()`, `hoursSummary()`, `myApplications()`, `myOrganisations()` | `src/routes/volunteering-actions.js`, `src/views/volunteering/hours.njk`, `src/lib/api.js` | Partial. Signed GET redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible hours summary, hours list, approved applications, and my-organisations endpoints, and renders the Blade-style back link, status states, auto-credit notice, stats, by-organisation/by-month tables, log-hours form, organisation/opportunity selectors, and recent hour-log cards. Runtime persistence, exact validation replay, tenant feature gate behavior, localization, and ASP.NET backend compatibility are not certified. |
| Volunteering organisation owner pages | `views/volunteering-org-dashboard.blade.php`, `views/volunteer-org-manage.blade.php`, `views/volunteering-org-settings.blade.php`, `views/volunteering-org-volunteers.blade.php`, `views/volunteering-org-wallet.blade.php`, `VolunteeringParity`, `AlphaController::manageVolunteerOrg()`, `VolunteerController` owner endpoints | `src/routes/volunteering-actions.js`, `src/views/volunteering/org-dashboard.njk`, `org-manage.njk`, `org-settings.njk`, `org-volunteers.njk`, `org-wallet.njk`, `src/lib/api.js` | Partial. Signed GETs redirect unsigned visitors to `/login?status=auth-required`, call Laravel owner-scoped stats, applications, pending-hours, volunteers, wallet summary, wallet transactions, and organisation detail endpoints, and render Blade-style dashboard stats, quick links, management review cards/forms, settings form, volunteers table, wallet auto-pay/deposit forms, transaction table, pagination, and status banners. Live owner authorization, tenant feature gates, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Organisations directory | `views/organisations.blade.php` | `src/views/organisations.njk` | Partial. Directory heading, subnavigation, search, empty state, registration copy, terms, and form are present. Directory data loads from Laravel `/api/v2/volunteering/organisations`; the embedded POST validates required fields/terms, requires a signed token, submits to Laravel `/api/v2/volunteering/organisations`, and uses Laravel status redirects. Subnavigation, search form, organisation links, and registration form now use `urlFor()` with source regression coverage. API-unavailable warning state is present; tenant/feature/runtime behavior is not certified. |
| Organisations browse | `views/organisations-browse.blade.php` | `src/views/organisations-browse.njk` | Partial. Caption, heading, search, empty state, organisation cards, public stats, website marker, and cursor load-more link are present. Data loads from Laravel `/api/v2/volunteering/organisations`; register/manage buttons, search form, organisation links, and load-more link now use `urlFor()` with source regression coverage. Auth/feature-gate/tenant behavior is not certified. |
| Organisations register | `views/organisations-register.blade.php` | `src/views/organisations-register.njk` | Partial. Back link, caption, standalone form, field hints, validation status anchors, terms, cancel link, and pending notice are present. POST validates Laravel field status keys, requires a signed token, submits to Laravel `/api/v2/volunteering/organisations`, and redirects to the directory success status. Back link, POST action, and cancel link now use `urlFor()` with source regression coverage. Feature gate, tenant routing, runtime persistence, and runtime behavior are not certified. |
| Organisations manage | `views/organisations-manage.blade.php` | `src/views/organisations-manage.njk` | Partial. Back link, caption, empty state, owner/admin cards, pending cards, dashboard links, and register CTA are present. Data loads from Laravel `/api/v2/volunteering/my-organisations` only when signed in; back/register/detail/manage links now use `urlFor()` with source regression coverage. Auth redirect, feature gate, tenant routing, and runtime behavior are not certified. |
| Organisation detail | `views/organisation-detail.blade.php` | `src/views/organisation-detail.njk` | Partial. Back link, caption, profile copy, contact summary, jobs link, basic public stats, active opportunity cards, apply links, review cards, rating progress, and empty depth fallbacks are present. Detail data loads from Laravel `/api/v2/volunteering/organisations/{id}?include=public_contract`, `/api/v2/volunteering/opportunities?organization_id={id}`, and `/api/v2/volunteering/reviews/organization/{id}`. Local organisation, volunteering opportunity, jobs, and apply links now use `urlFor()` with source regression coverage. |
| Organisation jobs | `views/organisations-jobs.blade.php` | `src/views/organisations-jobs.njk` | Partial. Back link, caption, heading, empty state, job cards, type tags, remote/location, deadline, and view-role links are present. Data loads from Laravel `/api/v2/jobs?organization_id={id}&status=open` only when signed in; auth redirect, feature gate, tenant routing, and runtime behavior are not certified. |
| Organisation opportunity apply | `views/organisations-apply.blade.php` | `src/views/organisations-apply.njk` | Partial. Back link, caption, summary list, already-applied state, auth-required notice, message field, notice, submit button, and cancel link are present. Data loads from Laravel `/api/v2/volunteering/opportunities/{id}`; apply POST, auth redirect, feature gate, tenant routing, and runtime behavior are not certified. |
| Contact and problem reporting | `views/contact.blade.php`, `views/report-problem.blade.php` | `src/views/contact.njk`, `src/views/report-problem.njk`, `src/routes/contact-support.js` | Partial. Contact GET/POST and report-problem GET/POST now use Blade-style forms, Laravel status keys, no-JS validation redirects, signed-out report-to-contact prefill, and Laravel `/api/v2/contact` plus `/api/v2/support/reports` submissions. Contact form/sign-in/home links, report-problem form/return link, and generic report-problem return links in the shared report-link partial now use `urlFor()` with source regression coverage. Tenant-domain routing, production Turnstile, localization, notification side effects, and ASP.NET backend compatibility are not certified. |
| Feed hashtag pages and item permalinks | `views/feed.blade.php`, `views/feed-hashtags.blade.php`, `views/feed-hashtag.blade.php`, `views/feed-post.blade.php`, `views/feed-item.blade.php`, `AlphaController::feed()`, `feedHashtagsDiscovery()`, `feedHashtag()`, `feedPost()`, `feedItemDetail()`, feed social APIs | `src/routes/feed.js`, `src/views/feed/*.njk`, `src/lib/api.js` | Partial. Signed GET `/feed` calls Laravel-compatible feed APIs and now normalizes both Laravel `author` post rows and older local `user` rows before rendering. Public GET `/feed/hashtags` calls Laravel-compatible hashtag trending/search APIs and renders the Blade-style discovery page. Public GET `/feed/hashtag/{tag}` calls Laravel-compatible `/api/v2/feed/hashtags/{tag}` and renders the Blade-style hashtag post list. Public GET `/feed/posts/{id}` calls Laravel-compatible `/api/v2/feed/posts/{id}` and renders the Blade-style post permalink. Public GET `/feed/item/{type}/{id}` calls Laravel-compatible `/api/v2/feed/items/{type}/{id}` and renders the Blade-style polymorphic permalink with item-type tag, title fallback, signed-out auth notice, media, deep link where available, engagement counts, and comments section. Feed templates now route local feed, member, group, login, pagination, and engagement controls through `urlFor()` with source regression coverage, focused render tests, and focused Laravel runtime smoke for `/feed`, `/feed/hashtags`, `/feed/hashtag/timebank`, `/feed/posts/796`, and `/feed/item/listing/42`. The local hashtag detail page uses the Laravel v2 cursor contract rather than the Blade controller's internal offset query. Tenant module gates, localization, signed reaction/share/save/comment UI depth, broader runtime behavior, and exact comment rendering are not certified. |
| Auth aliases | `views/forgot-password.blade.php`, `views/reset-password.blade.php`, `views/two-factor.blade.php`, `views/email-verify.blade.php`, `views/newsletter-unsubscribe.blade.php` | `src/views/forgot-password.njk`, `src/views/reset-password.njk`, `src/views/login.njk`, `src/views/public-info/email-verify.njk`, `src/views/public-info/newsletter-unsubscribe.njk`, `src/routes/auth.js`, `src/routes/public-info.js` | Partial. Laravel accessible aliases `/login/forgot-password`, `/password/reset`, `/login/two-factor`, and `/login/resend-verification` now route to local handlers with matching form actions/status redirects; reset submissions use Laravel's `password` and `password_confirmation` payload. Login, two-factor, forgot-password, reset-password, and register local links/forms now use `urlFor()` with source regression coverage. `/verify-email` renders Blade-style missing, invalid, and success states and calls Laravel `/api/auth/verify-email` when a token is present. `/newsletter/unsubscribe` renders Blade-style missing, invalid, and success states and calls Laravel `/api/v2/newsletter/unsubscribe` when a token is present. Localization, full Laravel status-copy parity, tenant-domain routing, and runtime behavior are not certified. |
| Marketplace commerce | `views/marketplace*.blade.php`, `AlphaController::marketplace*()`, marketplace parity concerns/controllers | `src/routes/marketplace*.js`, `src/views/marketplace/*.njk`, `src/lib/api.js` | Partial. Browse, detail, create/edit listing, my listings, saved/free/category/search, seller profile, offers, orders, sales, pickups, onboarding, pickup slots, coupons, buy, offer, and report pages render Laravel-backed Blade-style candidates through Laravel v2 marketplace APIs, with POST aliases for listing, offer, order, slot, coupon, and onboarding actions. The marketplace offers, my-listings, browse, detail, buyer-action, search, seller profile, onboarding, coupon, order, and pickup-slot templates now route offer tabs, listing links, offer decision forms, my-listings tabs, create/view/edit links, renew/delete forms, browse tabs, listing/card/category links, filter forms, detail buy/offer/save/report controls, buyer buy/offer/report forms, listing form actions, seller profile links, onboarding controls, coupon links/forms, order tabs/actions, and pickup-slot forms through `urlFor()` with source regression coverage. Marketplace action route redirects now route auth-required, validation, success, and API-failure exits through `res.locals.urlFor`, with shared-mount coverage for seller coupon validation. A source scan for raw marketplace-local `href`/`action` strings is clean. Hosted checkout redirect depth, merchant profile-image uploads, tenant feature gates, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Premium billing | `views/premium.blade.php`, `views/premium-return.blade.php`, `views/commerce-premium-manage.blade.php`, `AlphaController::premium*()`, `CommerceParity::premium*()` | `src/routes/premium.js`, `src/views/premium/*.njk`, `src/lib/api.js` | Partial. Signed pricing, manage, and return pages render Laravel-backed member-premium candidates through `/api/v2/member-premium/tiers` and `/api/v2/member-premium/me`; subscribe, portal, and cancel POST aliases call Laravel member-premium endpoints. Premium links/forms, route-level auth/status redirects, checkout `return_url`, and billing-portal `return_url` now route through `urlFor()`/`res.locals.urlFor` with source regression and shared-mount payload coverage. External Stripe checkout/portal runtime behavior, tenant premium feature gates, localization, exact billing wording, and ASP.NET backend compatibility are not certified. |
| Courses | `views/courses*.blade.php`, `AlphaController::courses*()`, course parity controllers | `src/routes/courses.js`, `src/views/courses/*.njk`, `src/lib/api.js` | Partial. Browse, detail, certificate, lesson player, my learning, instructor dashboard, create/edit builder, analytics, and grading pages render Laravel-backed Blade-style candidates through Laravel v2 course APIs, with POST aliases for enrolment, reviews, lesson completion, quizzes, instructor course, section, lesson, publish, delete, and grading actions. Course tabs, browse/search, course and prerequisite links, certificate and learning links, review/enrolment/quiz/progress forms, instructor create/edit analytics links, builder section/lesson forms, publish/unpublish/delete actions, and grading forms now route through `urlFor()` with source regression coverage. Course auth handoffs, certificate/learn errors, learner action results, instructor create/update/publish/unpublish/delete, section/lesson builder, and grading redirects now route through `res.locals.urlFor`. Tenant feature gates, localization, exact owner/instructor policy rendering, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Podcasts | `views/podcasts*.blade.php`, `PodcastParity` routes/controllers | `src/routes/podcasts.js`, `src/views/podcasts/*.njk`, `src/lib/api.js` | Partial. Browse, detail, episode, studio, create, and manage pages render Laravel-backed Blade-style candidates through Laravel v2 podcast APIs, with POST aliases for subscribe, show create/update/publish/delete, and episode add/publish/delete including multipart audio upload proxying. Podcast browse/detail/episode/form/manage/studio templates now route browse and studio links, search form, show and episode links, subscribe form, create/edit actions, episode management forms, show management forms, and studio management links through `urlFor()` with source regression coverage, focused render tests, and focused Laravel runtime smoke for `/podcasts`, `/podcasts/studio`, and `/podcasts/studio/new`. Tenant author gates, localization, broader runtime behavior, and ASP.NET backend compatibility are not certified. |
| Achievements | `views/achievements.blade.php`, `views/gamification-shop.blade.php`, `views/gamification-collections.blade.php`, `views/gamification-engagement.blade.php`, `views/gamification-showcase.blade.php`, `views/gamification-badge.blade.php`, `views/partials/gamification-nav.blade.php`, `AlphaController::achievements()`, `GamificationParity::gamificationShop()`, `GamificationParity::gamificationCollections()`, `GamificationParity::gamificationEngagement()`, `GamificationParity::gamificationShowcase()`, `GamificationParity::gamificationBadgeDetail()` | `src/routes/achievements.js`, `src/views/achievements/index.njk`, `src/views/achievements/shop.njk`, `src/views/achievements/collections.njk`, `src/views/achievements/engagement.njk`, `src/views/achievements/showcase.njk`, `src/views/achievements/badge.njk`, `src/lib/api.js` | Partial. `/achievements` redirects unsigned visitors to `/login?status=auth-required`, reads Laravel-compatible gamification profile, badges, badge progress, daily reward, and challenges, and renders the Blade-style gamification tabs, level/XP summary, level progress, daily reward claim form/status, active challenges with claim forms, earned badges, and badge-progress rows. `/achievements/shop` reads `/api/v2/gamification/shop` and renders the Blade-style back link, active shop tab, balance panel, warning, item cards, owned/out-of-stock/not-enough-XP states, and purchase forms. `/achievements/collections` reads `/api/v2/gamification/collections` and renders the Blade-style collection cards, progress bars, reward XP, completed/bonus tags, and earned/locked badge links. `/achievements/engagement` reads `/api/v2/gamification/engagement-history` and renders the Blade-style engagement history table with active/inactive tags and pluralized activity counts. `/achievements/showcase` reads `/api/v2/gamification/badges` and renders the Blade-style earned-badge checkbox management page, success/error status states, and save form. `/achievements/badges/{key}` reads `/api/v2/gamification/badges/{key}` and renders the Blade-style badge detail summary with earned status, metadata rows, showcased state, and view-all link. Gamification tabs, back links, daily reward/challenge/purchase/showcase forms, badge collection links, badge detail links, auth-required redirects, and POST result redirects now use `urlFor()`/`res.locals.urlFor`, with source-level regressions guarding the tenant-aware helper conversion. Exact tenant feature gate handling, localization beyond English strings, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Leaderboard and NEXUS score | `views/leaderboard.blade.php`, `views/gamification-competitive.blade.php`, `views/gamification-seasons.blade.php`, `views/gamification-journey.blade.php`, `views/gamification-spotlight.blade.php`, `views/nexus-score.blade.php`, `views/gamification-tiers.blade.php`, `views/partials/gamification-nav.blade.php`, `AlphaController::leaderboard()`, `AlphaController::nexusScore()`, `GamificationParity::gamificationCompetitive()`, `GamificationParity::gamificationSeasons()`, `GamificationParity::gamificationPersonalJourney()`, `GamificationParity::gamificationSpotlight()`, `GamificationParity::gamificationTierLadder()` | `src/routes/leaderboard.js`, `src/routes/nexus-score.js`, `src/views/leaderboard/index.njk`, `src/views/leaderboard/competitive.njk`, `src/views/leaderboard/seasons.njk`, `src/views/leaderboard/journey.njk`, `src/views/leaderboard/spotlight.njk`, `src/views/nexus-score/index.njk`, `src/views/nexus-score/tiers.njk`, `src/lib/api.js` | Partial. `/leaderboard` redirects unsigned visitors to `/login?status=auth-required`, reads Laravel-compatible `/api/v2/gamification/leaderboard` and `/api/v2/gamification/community-dashboard` data, and renders the Blade-style leaderboard tab strip, community impact stat grid, metric/period filter form, rank/member/score table, current-user tag, and empty state. `/leaderboard/competitive` reads Laravel-compatible `/api/v2/gamification/leaderboard` and `/api/v2/gamification/seasons/current` data and renders the Blade-style back link, active-season card, active leaderboard tabs, metric/period filter, rank banner, table, and load-more link. `/leaderboard/seasons` reads Laravel-compatible `/api/v2/gamification/seasons/current` and `/api/v2/gamification/seasons` data and renders the Blade-style current-season card, rewards, season leaders, and past-seasons table. `/leaderboard/journey` reads Laravel-compatible `/api/v2/gamification/personal-journey` data and renders the Blade-style summary list, milestones, monthly activity table, badge progression list, and empty states. `/leaderboard/spotlight` reads Laravel-compatible `/api/v2/gamification/member-spotlight` data and renders the Blade-style daily featured-member card list and empty state. `/nexus-score` and `/nexus-score/tiers` read Laravel-compatible `/api/v2/gamification/nexus-score` data and render the Blade-style overview, tier-ladder related link, score panel, breakdown progress table, insights list, nine-tier ladder, current tier, points-to-next inset, reached/current/locked statuses, and unavailable-score states. Leaderboard tabs, back links, filter form actions, load-more links, NEXUS tier links, and data-driven member profile links now use `urlFor()`, with a source-level regression guarding the tenant-aware helper conversion. Exact metric formatting for every legacy service type, feature gates, localization beyond English strings, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Reviews | `views/reviews.blade.php`, `views/blogreviews-reviews-list.blade.php`, `views/blogreviews-review-comments.blade.php`, `AlphaController::reviews()`, `BlogReviewsParity::blogReviewsList()`, `BlogReviewsParity::blogReviewsReviewComments()` | `src/routes/reviews.js`, `src/views/reviews/index.njk`, `src/views/reviews/list.njk`, `src/views/reviews/comments.njk`, `src/lib/api.js` | Partial. Signed `/reviews` reads Laravel-compatible received, given, pending, and stats review endpoints and renders the Blade-style summary, review-score panel, received/given cards, pending review forms, and Laravel-matched status presentation: submitted/deleted statuses use success notification banners, while invalid/duplicate/failed/delete-failed statuses use the GOV.UK error summary title and Laravel English copy. Signed `/reviews/list` renders the received/given paginated list with Laravel cursor links. Signed `/reviews/{id}/comments` reads the review, comments, and reaction summary, then renders the Blade-style discussion page, fixed reaction set, comment list, and no-JS comment form. POST aliases cover review creation, comments, and reactions, and route-generated auth/result redirects now pass through `res.locals.urlFor` for tenant mounts. Exact feature gates, deletion/moderation display, threaded reply depth, localization beyond the matched English strings, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Members discovery | `views/members-discover.blade.php`, `AlphaController::membersDiscover()`, `Api\UsersController::index()` | `src/routes/members.js`, `src/views/members/discover.njk`, `src/lib/api.js` | Partial. Signed GET `/members/discover` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible `/api/v2/users?sort=communityrank`, and renders the Blade-style recommended-members page with back link, community caption, filter navigation, search form, result count, member cards, recommendation score progress, profile links, and load-more link. Discovery links/forms/profile targets and load-more links now use `urlFor()` with source regression, focused render coverage, and focused Laravel runtime smoke. Exact ranking-enabled fallback copy, tenant feature gates, localization, live ranking behaviour, insights page linkage, and ASP.NET backend compatibility are not certified. |
| Members nearby | `views/members-nearby.blade.php`, `AlphaController::membersNearby()`, `Api\UsersController::nearby()` | `src/routes/members.js`, `src/views/members/nearby.njk`, `src/lib/api.js` | Partial. Signed GET `/members/nearby` redirects unsigned visitors to `/login?status=auth-required`, reads the signed-in profile location, calls Laravel-compatible `/api/v2/members/nearby`, and renders the Blade-style nearby-members page with back link, community caption, filter navigation, no-location inset, radius/search form, distance cards, profile links, and load-more link. Nearby links/forms/profile targets and load-more links now use `urlFor()` with source regression, focused render coverage, and focused Laravel runtime smoke. Exact tenant feature gates, localization, live privacy/visibility gating, and ASP.NET backend compatibility are not certified. |
| Members insights | `views/members-insights.blade.php`, `AlphaController::membersInsights()`, `Api\UsersController::show()`, `Api\MemberVerificationBadgeController::getUserBadges()` | `src/routes/members.js`, `src/views/members/insights.njk`, `src/lib/api.js` | Partial. Signed GET `/members/{id}/insights` redirects unsigned visitors to `/login?status=auth-required`, calls Laravel-compatible profile, `/api/v2/users/{id}`, and `/api/v2/users/{id}/verification-badges` endpoints, and renders the Blade-style reputation page with NEXUS score, tier, percentile progress, activity stats, verification badges, earned badges, and profile back links. Profile back links now use `urlFor()` with source regression, focused render coverage, and focused Laravel runtime smoke. Member action auth/status redirects now use `res.locals.urlFor` for shared mounts and custom-domain contexts. Exact tenant feature gates, block/privacy/onboarding gating, localization, runtime behavior, and ASP.NET backend compatibility are not certified. |
| Generated Laravel GET preparation fallback | Laravel route matrix | `src/routes/laravel-prep-pages.js` and `src/views/static-page.njk` | Started. The fallback router reads `docs/generated/accessible-route-matrix.json` and registers Laravel GET paths after real routes. The current generated matrix has zero `static-page` rows, so no Laravel GET route is presently being handled by this preparation fallback. Keeping the fallback in place preserves route discoverability if future Laravel routes are added, but it does not certify Blade visual parity, backend data, forms, redirects, tenant routing, auth, feature gates, localization, or POST workflows. |

Feed action redirect note: `src/routes/feed-actions.js` now sends feed post,
item, comment, poll, moderation, share, save, and mute POST result redirects
through `res.locals.urlFor`, with focused shared-mount validation redirect
coverage for `/acme/accessible/feed/posts`.

Group-exchange action redirect note: `src/routes/group-exchange-actions.js`
now sends auth, validation, success, failure, and API-auth-error POST redirects
through `res.locals.urlFor`, with focused shared-mount validation redirect
coverage for `/acme/accessible/group-exchanges/new`.

Group-exchange GET redirect note: `src/routes/group-exchanges.js` now sends
signed-out list, create, and detail auth handoffs through `res.locals.urlFor`,
with focused shared-mount coverage for `/acme/accessible/group-exchanges`,
`/acme/accessible/group-exchanges/new`, and
`/acme/accessible/group-exchanges/7`.

Group route redirect note: `src/routes/groups.js` now sends central group
action auth and API-failure redirects plus file-download auth handoffs through a
route-local helper backed by `res.locals.urlFor`, with focused source coverage
and shared-mount notification failure coverage guarding against flat or
double-prefixed redirects.

Ideation action redirect note: `src/routes/ideation-actions.js` now sends
challenge, idea, outcome, media, conversion, and campaign POST redirects
through `res.locals.urlFor`, with focused shared-mount create redirect coverage
for `/acme/accessible/ideation/new`.

Ideation source-template note: `src/views/ideation/*.njk` now routes local
tabs, links, filters, and form actions through `urlFor()`, so the source no
longer embeds flat `/ideation` links before shared-mount/custom-domain
rewriting.

## Still To Port

| Blade pattern | Why it matters | Suggested ASP.NET implementation |
| --- | --- | --- |
| Localization completion and RTL certification | Structural catalogs and a language selector do not prove translated, context-correct pages or bidirectional accessibility. | Keep `npm run locales:audit`, `npm run locales:audit-templates -- --summary`, and the expanded 12-case browser gate green, then review contextual route titles, headings, status/validation copy, ARIA labels, and residual templates family by family. The read-only Laravel owner must supply authoritative translations for the 16 wholly English-identical namespaces rather than Web UK inventing divergent copy. Complete a recorded manual keyboard, screen-reader, focus, reflow, contrast, and RTL pass before marking this row complete. |
| Tenant logo and shape sizing | Blade supports tenant-uploaded dark logos with aspect-ratio-aware sizing. | Add tenant logo locals after ASP.NET tenant bootstrap exposes compatible logo URLs and validated header colours. |
| Per-tenant header colours | Blade safely validates `#rrggbb` values and picks readable foreground colours. | Add validated colour locals only after tenant setting parity exists. |
| Cookie consent depth | Blade records choices through `CookieConsentService` and tenant-scoped consent audit storage. | Certify tenant scoping, backend audit persistence, localization, route-name generation, and runtime behavior before shared use. |
| Account hub depth | Blade also conditionally includes matches, group exchanges, gamification, linked accounts, appearance, saved items, reviews, activity, jobs, and other feature-gated links. | Add missing account cards only after each target route and Laravel-compatible backend contract is certified. |
| Module and feature gating | Blade only shows modules enabled for the tenant. | The shared service navigation, footer Platform column, and Explore card list now consume tenant bootstrap `modules`/`features` with Laravel-style fallback semantics. Continue certifying page-level disabled states, account hub cards, live disabled-tenant broker workflow data, and module route redirects/errors family by family; clubs active-club routing and Explore-card sourcing now have focused proof. |
| Live Explore content | Blade shows recent listings and upcoming events. | The protected `/explore` route now reads Laravel `/api/v2/explore` and renders listing/event summaries when returned. Its listing/event links and view-all links now use `urlFor()` so shared mounts and custom-domain contexts do not leak flat root paths, and the Clubs card uses the same Laravel-backed active-club evidence as the `/clubs` route. Exact Blade service-source parity, live disabled-tenant broker workflow proof, feature-disabled page behavior, localization, and broader runtime certification still need work. |
| Volunteering workflows | Blade protects the page with auth, volunteering feature checks, applications, recommendations, hours, owner tools, opportunity details, shifts, credentials, certificates, accessibility needs, wellbeing, donations, expenses, emergency alerts, group sign-ups, safeguarding training/incidents, waitlists, swaps, and POST workflows. | The public `/volunteering` landing/search GET and `/volunteering/opportunities/{id}` detail GET now read Laravel-compatible opportunities data. Signed `/volunteering/accessibility`, `/volunteering/certificates`, `/volunteering/certificates/{code}/download`, `/volunteering/credentials`, `/volunteering/hours`, `/volunteering/wellbeing`, `/volunteering/donations`, `/volunteering/expenses`, `/volunteering/emergency-alerts`, `/volunteering/group-signups`, `/volunteering/training`, `/volunteering/incidents`, `/volunteering/waitlist`, `/volunteering/swaps`, `/volunteering/my-organisations`, `/volunteering/recommended-shifts`, and `/volunteering/opportunities/create` GETs read Laravel-compatible data and render Blade-style forms/cards/tables/dashboard states. The recommended-shifts source template routes its volunteering back link and opportunity links through `urlFor()` with source regression coverage; certificate/credential templates now route their local back, generate/download, upload, and delete controls through `urlFor()`. POST aliases now cover applications, shift signup/cancel, hours, accessibility, certificates, swaps, emergency alert responses, credential upload/delete, wellbeing, donations, group reservations, expenses, training, incidents, opportunity creation, and owner actions through Laravel-compatible APIs. The current Laravel semantic refresh resolves and uppercases tenant donation currency with EUR fallback, omits currency from donation POST, rejects amounts above `1,000,000`, distinguishes the two `Warning` advisories from real error summaries, and links five safeguarding field failures to their controls while leaving two generic failures as text. Focused source/render/action tests cover those contracts, including a GBP tenant render; live non-EUR persistence still needs a disposable fixture because none of the 15 local tenant bootstraps exposes a non-EUR currency. Credential download, tenant routing, auth redirects, and feature gates still need certification. |
| Organisations workflows | Blade protects the page with auth, volunteering, and job-vacancy feature checks, lists real organisations, shows detail depth data, posts registrations through `VolunteerService`, reads organisation jobs through `JobVacancyService`, and posts opportunity applications to the existing volunteering apply route. | Directory, browse, manage, detail, organisation jobs, and organisation opportunity apply GET data now read Laravel-compatible APIs. Register GET and both organisation registration POST routes are ported against the Laravel API/status contract with mocked tests. Organisation directory/detail/manage/register/jobs/apply source templates now route local links/forms through `urlFor()`. Tenant routing, full auth redirects, feature gates, apply POST workflow, runtime persistence, and runtime smoke certification still need work. |
| Blog workflows | Blade feature-gates blog availability, renders public listing/detail pages, requires auth for discussion and likers pages, and wires comments/reactions through Laravel services. | Blog listing/detail/discussion/likers GET pages and comment/reaction POST aliases now call Laravel-compatible `/api/v2/blog`, `/api/v2/comments`, and `/api/v2/reactions` endpoints. Blog index/detail/comments/likers source templates now route local blog/member links, pagination, comment/reaction forms, and liker controls through `urlFor()` with source regression coverage, and blog route-level redirects now use `res.locals.urlFor` for auth handoffs and POST results. Feature gates, exact tenant captions, localization, RSS metadata depth, comment authorization depth, runtime persistence, and smoke certification still need work. |
| Poll workflows | Blade feature-gates polls, requires auth, splits open and closed polls, supports category/my-polls filters, ranked choice, result export, likes, and comments. | Poll listing/detail/ranked/create/manage/export GET pages and existing POST aliases now call Laravel-compatible `/api/v2/polls`, `/api/v2/comments`, and feed-like endpoints. POST aliases and the legacy poll vote route now keep auth-required and result redirects tenant-aware through `res.locals.urlFor`. Feature gates, exact service-level open/closed list parity, owner authorization depth, localization, runtime persistence, and smoke certification still need work. |
| Jobs workflows | Blade feature-gates jobs, renders browse/saved/applications/owner/employer/talent pages, and wires applications, alerts, interviews, offers, CV downloads, and owner controls through Laravel jobs services. | Jobs browse/detail/saved/applications/mine/create/edit/owner-applicants/analytics/pipeline/qualified/alerts/responses/talent-search/talent-profile/employer-brand/employer-onboarding/bias-audit GET pages and POST aliases now call Laravel-compatible jobs/admin/user APIs. Jobs source templates route tabs, browse filters, saved/application/owner links, alerts, responses, detail save/apply/renew forms, employer-brand links, talent search/profile links, CSV/CV downloads, pagination, and variable form targets through `urlFor()`. The contextual localization slice adds 16 exact fixed-title keys, render-time translation for the browse/saved/application/owner/form/alert/response/pipeline/applicant/detail status families, and Blade-aligned detail/history/talent/bias/qualification copy while preserving dynamic user content. Every literal key resolves through the 11 imported catalogs. Focused/shared Jest passed 58 tests; fresh ephemeral Laravel runs passed 41/41 Jobs page/body/gate/redirect checks and 13/13 Irish/Arabic rendered-output checks. Create/edit API validation still needs Laravel-style one-request input/error replay. Full Jobs upload/destructive persistence, every residual template string, tenant feature-gate depth, visual/manual accessibility, and ASP.NET backend compatibility are not certified. |
| Podcast workflows | Blade renders podcast browse, show, episode, studio, create, and manage pages, with author-only studio actions and multipart episode audio upload support through Laravel services/APIs. | Podcast GET pages and POST aliases now call Laravel-compatible podcast APIs. Podcast source templates now route browse/studio links, search, show/episode navigation, subscribe action, create/edit actions, episode add/publish/delete/upload actions, show publish/delete actions, and studio management links through `urlFor()` with source regression, focused render, and focused Laravel runtime smoke coverage. Tenant author gates, localization, runtime persistence depth, and ASP.NET backend compatibility are not certified. |
| Goals workflow depth | Blade feature-gates goals, requires auth, renders index/detail/edit/templates/buddy/discover/depth pages, and wires check-ins, reminders, buddy actions, history, and social interactions. | `/goals` now renders the signed Laravel-backed index, `/goals/templates` renders the signed Blade-style template picker through Laravel-compatible template/category APIs, `/goals/discover` renders the signed Blade-style public buddy-goal discovery page through Laravel-compatible discover APIs, `/goals/buddying` renders the signed Blade-style goals-you-buddy and public-goals-looking-for-a-buddy sections through Laravel-compatible mentoring/discover APIs, `/goals/{id}/edit` renders the signed Blade-style owner edit form from Laravel-compatible goal detail data, `/goals/{id}/checkin` renders the signed Blade-style progress/mood/note check-in form and recent history from Laravel-compatible goal and check-in APIs, `/goals/{id}/reminder` renders the signed Blade-style reminder status and settings form from Laravel-compatible goal and reminder APIs, `/goals/{id}/buddy-actions` renders the signed Blade-style buddy support form from Laravel-compatible goal detail data, `/goals/{id}/history` renders the signed Blade-style chronological progress timeline from Laravel-compatible goal detail and history APIs, `/goals/{id}/insights` renders the signed Blade-style streak/cadence/milestone/buddy-support summary from Laravel-compatible goal detail and insights APIs, `/goals/{id}/social` renders the signed Blade-style support toggle, like/comment counts, threaded comments, reply/delete controls, status banners, validation error, and add-comment form from Laravel-compatible goal/social/comment APIs, and existing POST aliases call Laravel-compatible goals/template/comment/like APIs. Goal templates now route local links/forms through `urlFor()`, and route-generated auth/status redirects now pass through `res.locals.urlFor` with shared-mount coverage for create, buddy-nudge, comment, and unsigned progress redirects. The detail GET page still needs Blade rendering and runtime certification. |
| Public coupon browsing | Blade feature-gates merchant coupons, requires auth, renders the public `/coupons` list and `/coupons/{id}` detail pages from `MerchantCouponService`, and exposes coupon code/redemption guidance. | `/coupons` and `/coupons/{id}` now render signed Laravel-backed public coupon pages through `/api/v2/coupons` and `/api/v2/coupons/{id}`. Public coupon list/detail links now use `urlFor()` with source regression and focused render coverage. Route-level coupon auth redirects now use `res.locals.urlFor`, with shared-mount coverage proving `/acme/accessible/coupons` and `/acme/accessible/coupons/{id}` redirect to the tenant-mounted login path. Scoped Laravel runtime smoke proves the current local fixture returns expected `403` merchant-coupons feature gates for `/coupons` and `/coupons/1`; rendered coupon body parity still needs runtime proof in a merchant-coupons-enabled tenant. Exact tenant captions, QR redemption/validation workflows, localization, runtime persistence, and ASP.NET backend compatibility are not certified. |
| Clubs workflow depth | Blade exposes `/clubs` only for tenants that have active club organisations and renders a Laravel DB-backed directory. | `/clubs` now renders a signed Laravel-backed club directory through `/api/v2/clubs`; the no-active-club tenant gate returns `404`, searched empty results remain renderable when active-club evidence exists, and the default Laravel runtime smoke checks the local `hour-timebank` fixture as signed gated `/clubs` `404`. Exact tenant captions, localization, runtime persistence depth, and ASP.NET backend compatibility are not certified. |
| Skills workflow depth | Blade renders the signed-in taxonomy directory from `SkillTaxonomyService`, with category drill-in and skill member search. | `/skills` now renders a signed Laravel-backed taxonomy directory through `/api/v2/skills/*`, with source links/forms and auth redirects converted to tenant-aware helpers; exact tenant captions, localization, category/member authorization edge cases, runtime persistence, and smoke certification still need work. |
| Resource action depth | Blade has a simple `/resources` list plus a richer `/resources/library`, upload, delete confirmation, download, comments, reactions, and admin reorder views. | `/resources`, `/resources/library`, `/resources/upload`, `/resources/{id}/delete`, `/resources/{id}/download`, `/resources/{id}/comments`, and the existing POST aliases now call Laravel-compatible resource APIs, including multipart upload proxying and admin reorder UI/action controls. Resource browse/library/upload/delete/comments source templates now route local resource links/forms through `urlFor()` for tenant-aware shared-mount and custom-domain rendering, and resource route-level redirects now use `res.locals.urlFor` for auth, upload, reorder, delete, comment, and reaction handoffs with focused shared-mount delete redirect coverage. Focused Laravel runtime smoke covers the GET resource pages. Feature gates, localization, and live upload POST certification still need work. |
| Page-specific subnavs | Commerce, federation, gamification, jobs, messages, and ideation have Blade partial navs. | Port each partial into Nunjucks during module-by-module route parity work. |
| Error and validation summaries | Blade uses consistent GOV.UK error summary behavior. | Audit every form after route/workflow parity is mapped. |

## Do Not Port

- GOV.UK crown, logotype, official header identity, OGL block, or Crown
  copyright wording.
- Laravel/PHP implementation details.
- Production traffic routing before certification.
- ASP.NET-specific visual inventions that diverge from the Blade source.
