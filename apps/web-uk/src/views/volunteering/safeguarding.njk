{# Copyright (c) 2024-2026 Jasper Ford #}
{# SPDX-License-Identifier: AGPL-3.0-or-later #}
{# Author: Jasper Ford #}
{# See NOTICE file for attribution and acknowledgements. #}
{% extends "layouts/base.njk" %}

{% block content %}
  <a class="govuk-back-link" href="{{ urlFor('/volunteering') }}">Back to volunteering</a>

  {% if status %}
    {% if status.type == "success" %}
      <div class="govuk-notification-banner govuk-notification-banner--success" data-module="govuk-notification-banner" role="alert" aria-labelledby="safeguarding-success-title">
        <div class="govuk-notification-banner__header">
          <h2 class="govuk-notification-banner__title" id="safeguarding-success-title">Success</h2>
        </div>
        <div class="govuk-notification-banner__content">
          <p class="govuk-notification-banner__heading">{{ status.message }}</p>
        </div>
      </div>
    {% else %}
      <div class="govuk-error-summary" data-module="govuk-error-summary" tabindex="-1">
        <div role="alert">
          <h2 class="govuk-error-summary__title">There is a problem</h2>
          <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list">
              <li>{{ status.message }}</li>
            </ul>
          </div>
        </div>
      </div>
    {% endif %}
  {% endif %}

  <span class="govuk-caption-l">Volunteering</span>
  <h1 class="govuk-heading-xl">Safeguarding</h1>
  <p class="govuk-body-l">Log your safeguarding training and report any concerns or incidents. All information is handled confidentially by your community administrators.</p>

  {% if loadError %}
    <div class="govuk-error-summary" data-module="govuk-error-summary" tabindex="-1">
      <div role="alert">
        <h2 class="govuk-error-summary__title">There is a problem</h2>
        <div class="govuk-error-summary__body">
          <p>{{ loadError }}</p>
        </div>
      </div>
    </div>
  {% endif %}

  <nav class="govuk-!-margin-bottom-6" aria-label="Safeguarding sections">
    <ul class="govuk-list" style="display:flex;gap:1rem;flex-wrap:wrap;list-style:none;padding:0;margin:0;">
      <li>
        <a class="govuk-link{% if subView == 'training' %} govuk-link--no-visited-state{% endif %}" href="{{ urlFor('/volunteering/training') }}"{% if subView == "training" %} aria-current="page"{% endif %}>Training records</a>
      </li>
      <li>
        <a class="govuk-link{% if subView == 'incidents' %} govuk-link--no-visited-state{% endif %}" href="{{ urlFor('/volunteering/incidents') }}"{% if subView == "incidents" %} aria-current="page"{% endif %}>Incident reports</a>
      </li>
    </ul>
  </nav>

  {% if subView == "training" %}
    <h2 class="govuk-heading-m">Log a training record</h2>
    <form method="post" action="{{ urlFor('/volunteering/training') }}" novalidate>
      <input type="hidden" name="_csrf" value="{{ csrfToken }}">

      <div class="govuk-form-group{% if status.field == 'training_type' %} govuk-form-group--error{% endif %}">
        <label class="govuk-label govuk-label--s" for="training_type">Training type</label>
        {% if status.field == "training_type" %}
          <p id="training-type-error" class="govuk-error-message"><span class="govuk-visually-hidden">Error:</span> {{ status.message }}</p>
        {% endif %}
        <select class="govuk-select{% if status.field == 'training_type' %} govuk-select--error{% endif %}" id="training_type" name="training_type"{% if status.field == "training_type" %} aria-describedby="training-type-error"{% endif %}>
          <option value="">Choose a training type</option>
          {% for type in trainingTypes %}
            <option value="{{ type.value }}">{{ type.label }}</option>
          {% endfor %}
        </select>
      </div>

      <div class="govuk-form-group{% if status.field == 'training_name' %} govuk-form-group--error{% endif %}">
        <label class="govuk-label govuk-label--s" for="training_name">Course or programme name</label>
        {% if status.field == "training_name" %}
          <p id="training-name-error" class="govuk-error-message"><span class="govuk-visually-hidden">Error:</span> {{ status.message }}</p>
        {% endif %}
        <input class="govuk-input{% if status.field == 'training_name' %} govuk-input--error{% endif %}" id="training_name" name="training_name" type="text" maxlength="255"{% if status.field == "training_name" %} aria-describedby="training-name-error"{% endif %}>
      </div>

      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--s" for="provider">Training provider <span class="govuk-hint">Optional</span></label>
        <input class="govuk-input" id="provider" name="provider" type="text" maxlength="255">
      </div>

      <div class="govuk-form-group{% if status.field == 'completed_at' %} govuk-form-group--error{% endif %}">
        <label class="govuk-label govuk-label--s" for="completed_at">Date completed</label>
        {% if status.field == "completed_at" %}
          <p id="completed-at-error" class="govuk-error-message"><span class="govuk-visually-hidden">Error:</span> {{ status.message }}</p>
        {% endif %}
        <input class="govuk-input govuk-input--width-10{% if status.field == 'completed_at' %} govuk-input--error{% endif %}" id="completed_at" name="completed_at" type="date"{% if status.field == "completed_at" %} aria-describedby="completed-at-error"{% endif %}>
      </div>

      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--s" for="expires_at">Expiry date <span class="govuk-hint">Optional</span></label>
        <input class="govuk-input govuk-input--width-10" id="expires_at" name="expires_at" type="date">
      </div>

      <button type="submit" class="govuk-button govuk-!-margin-top-2">Log training record</button>
    </form>

    <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">

    <h2 class="govuk-heading-m">Your training records</h2>
    {% if not trainings.length %}
      <div class="govuk-inset-text">You have not logged any training records yet.</div>
    {% else %}
      <table class="govuk-table">
        <caption class="govuk-table__caption govuk-visually-hidden">Your training records</caption>
        <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th class="govuk-table__header" scope="col">Course name</th>
            <th class="govuk-table__header" scope="col">Type</th>
            <th class="govuk-table__header" scope="col">Completed</th>
            <th class="govuk-table__header" scope="col">Expires</th>
            <th class="govuk-table__header" scope="col">Status</th>
          </tr>
        </thead>
        <tbody class="govuk-table__body">
          {% for training in trainings %}
            <tr class="govuk-table__row">
              <td class="govuk-table__cell">
                <strong>{{ training.trainingName }}</strong>
                {% if training.provider %}
                  <br><span class="govuk-hint govuk-!-font-size-16">{{ training.provider }}</span>
                {% endif %}
              </td>
              <td class="govuk-table__cell">{{ training.type.label }}</td>
              <td class="govuk-table__cell">{{ training.completedAtLabel or "-" }}</td>
              <td class="govuk-table__cell">{{ training.expiresAtLabel or "-" }}</td>
              <td class="govuk-table__cell"><strong class="govuk-tag {{ training.status.className }}">{{ training.status.label }}</strong></td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% endif %}
  {% else %}
    <h2 class="govuk-heading-m">Report a safeguarding concern or incident</h2>
    <div class="govuk-inset-text" role="note">
      Use this form to report a concern, allegation, near-miss, or any incident that may relate to the safety or welfare of a person. Your report will be reviewed confidentially by a designated person.
    </div>

    <form method="post" action="{{ urlFor('/volunteering/incidents') }}" novalidate>
      <input type="hidden" name="_csrf" value="{{ csrfToken }}">

      <div class="govuk-form-group{% if status.field == 'title' %} govuk-form-group--error{% endif %}">
        <label class="govuk-label govuk-label--s" for="title">Brief title</label>
        {% if status.field == "title" %}
          <p id="incident-title-error" class="govuk-error-message"><span class="govuk-visually-hidden">Error:</span> {{ status.message }}</p>
        {% endif %}
        <input class="govuk-input{% if status.field == 'title' %} govuk-input--error{% endif %}" id="title" name="title" type="text" maxlength="255"{% if status.field == "title" %} aria-describedby="incident-title-error"{% endif %}>
      </div>

      <div class="govuk-form-group{% if status.field == 'description' %} govuk-form-group--error{% endif %}">
        <label class="govuk-label govuk-label--s" for="description">Description</label>
        <div id="description-hint" class="govuk-hint">Describe what happened, when, and who was involved. Be as specific as possible. Minimum 20 characters.</div>
        {% if status.field == "description" %}
          <p id="incident-desc-error" class="govuk-error-message"><span class="govuk-visually-hidden">Error:</span> {{ status.message }}</p>
        {% endif %}
        <textarea class="govuk-textarea{% if status.field == 'description' %} govuk-textarea--error{% endif %}" id="description" name="description" rows="5" maxlength="2000" aria-describedby="description-hint{% if status.field == 'description' %} incident-desc-error{% endif %}"></textarea>
      </div>

      <div class="govuk-form-group">
        <fieldset class="govuk-fieldset">
          <legend class="govuk-fieldset__legend govuk-fieldset__legend--s">Severity</legend>
          <div class="govuk-radios govuk-radios--inline">
            {% for severity in severities %}
              <div class="govuk-radios__item">
                <input class="govuk-radios__input" id="severity-{{ severity.value }}" name="severity" type="radio" value="{{ severity.value }}"{% if severity.value == "low" %} checked{% endif %}>
                <label class="govuk-label govuk-radios__label" for="severity-{{ severity.value }}">{{ severity.label }}</label>
              </div>
            {% endfor %}
          </div>
        </fieldset>
      </div>

      <div class="govuk-form-group">
        <label class="govuk-label govuk-label--s" for="category">Category <span class="govuk-hint">Optional</span></label>
        <input class="govuk-input govuk-input--width-20" id="category" name="category" type="text" maxlength="100">
      </div>

      <button type="submit" class="govuk-button govuk-button--warning govuk-!-margin-top-2">Submit report</button>
    </form>

    <hr class="govuk-section-break govuk-section-break--m govuk-section-break--visible">

    <h2 class="govuk-heading-m">Your submitted reports</h2>
    {% if not incidents.length %}
      <div class="govuk-inset-text">You have not submitted any incident reports.</div>
    {% else %}
      <table class="govuk-table">
        <caption class="govuk-table__caption govuk-visually-hidden">Your submitted reports</caption>
        <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th class="govuk-table__header" scope="col">Title</th>
            <th class="govuk-table__header" scope="col">Severity</th>
            <th class="govuk-table__header" scope="col">Status</th>
            <th class="govuk-table__header" scope="col">Reported</th>
          </tr>
        </thead>
        <tbody class="govuk-table__body">
          {% for incident in incidents %}
            <tr class="govuk-table__row">
              <td class="govuk-table__cell">
                <strong>{{ incident.title }}</strong>
                {% if incident.category and incident.category != "general" %}
                  <br><span class="govuk-hint govuk-!-font-size-16">{{ incident.category }}</span>
                {% endif %}
                {% if incident.descriptionExcerpt %}
                  <br><span class="govuk-hint govuk-!-font-size-16">{{ incident.descriptionExcerpt }}</span>
                {% endif %}
              </td>
              <td class="govuk-table__cell"><strong class="govuk-tag {{ incident.severity.className }}">{{ incident.severity.label }}</strong></td>
              <td class="govuk-table__cell"><strong class="govuk-tag {{ incident.status.className }}">{{ incident.status.label }}</strong></td>
              <td class="govuk-table__cell">{{ incident.createdAtLabel or "-" }}</td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% endif %}
  {% endif %}
{% endblock %}
