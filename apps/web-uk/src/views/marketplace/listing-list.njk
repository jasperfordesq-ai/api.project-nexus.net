{# Copyright (c) 2024-2026 Jasper Ford #}
{# SPDX-License-Identifier: AGPL-3.0-or-later #}
{# Author: Jasper Ford #}
{# See NOTICE file for attribution and acknowledgements. #}

{% extends "layouts/base.njk" %}

{% block mainContent %}
  {% if backHref %}
    <a class="govuk-back-link" href="{{ urlFor(backHref) }}">{{ t("govuk_alpha_commerce.category.back_to_marketplace") if mode == 'category' else (backLabel or 'Back to marketplace') }}</a>
  {% endif %}

  {% include "marketplace/_nav.njk" %}

  {% if mode == 'saved' %}
    <span class="govuk-caption-xl">{{ t("govuk_alpha_commerce.saved.caption", { community: tenantName or serviceName }) }}</span>
    <h1 class="govuk-heading-xl">{{ t("govuk_alpha_commerce.saved.title") }}</h1>
    <p class="govuk-body-l">{{ t("govuk_alpha_commerce.saved.description") }}</p>

    {% if status %}
      <div class="govuk-notification-banner govuk-notification-banner--success" role="region" aria-labelledby="commerce-saved-status" data-module="govuk-notification-banner">
        <div class="govuk-notification-banner__header">
          <h2 class="govuk-notification-banner__title" id="commerce-saved-status">{{ t("govuk_alpha_commerce.common.notice_title") }}</h2>
        </div>
        <div class="govuk-notification-banner__content">
          <p class="govuk-notification-banner__heading">{{ status.message }}</p>
        </div>
      </div>
    {% endif %}
  {% elseif mode == 'free' %}
    <span class="govuk-caption-xl">{{ t("govuk_alpha_commerce.free_items.caption", { community: tenantName or serviceName }) }}</span>
    <h1 class="govuk-heading-xl">{{ t("govuk_alpha_commerce.free_items.title") }}</h1>
    <p class="govuk-body-l">{{ t("govuk_alpha_commerce.free_items.description") }}</p>
  {% elseif mode == 'category' %}
    <span class="govuk-caption-l">{{ t("govuk_alpha_commerce.category.caption", { community: tenantName or serviceName }) }}</span>
    <h1 class="govuk-heading-xl">{{ category.name }}</h1>
    <p class="govuk-body">{{ tc("govuk_alpha_commerce.category.count", listings.length, { count: listings.length }) }}</p>
  {% else %}
    {% include "marketplace/_status-banner.njk" %}
    <span class="govuk-caption-xl">{{ caption }} at {{ tenantName or serviceName }}</span>
    <h1 class="govuk-heading-xl">{{ heading }}</h1>
    <p class="govuk-body-l">{{ description }}</p>
  {% endif %}

  {% if mode == 'category' %}
    <form method="get" action="{{ urlFor('/marketplace/category/' + category.slug) }}" class="govuk-!-margin-bottom-6">
      <div class="govuk-form-group">
        <label class="govuk-label" for="q">{{ t("govuk_alpha_commerce.category.search_label") }}</label>
        <div id="q-hint" class="govuk-hint">{{ t("govuk_alpha_commerce.category.search_hint") }}</div>
        <input class="govuk-input govuk-input--width-20" id="q" name="q" type="search" value="{{ query }}" aria-describedby="q-hint">
      </div>
      <button type="submit" class="govuk-button govuk-button--secondary" data-module="govuk-button">{{ t("govuk_alpha_commerce.category.search_submit") }}</button>
    </form>
  {% endif %}

  {% if not listings.length %}
    {% if mode == 'category' %}
      <p class="govuk-inset-text">{{ t("govuk_alpha_commerce.category.empty") }}</p>
    {% else %}
      <div class="govuk-inset-text">
        <p class="govuk-body">{% if mode == 'saved' %}{{ t("govuk_alpha_commerce.saved.empty") }}{% elseif mode == 'free' %}{{ t("govuk_alpha_commerce.free_items.empty") }}{% else %}{{ emptyMessage }}{% endif %}</p>
      </div>
    {% endif %}
  {% else %}
    <div class="nexus-alpha-card-list">
      {% for item in listings %}
        {% include "marketplace/_listing-card.njk" %}
        {% if mode == 'saved' %}
          <form method="post" action="{{ urlFor('/marketplace/' + (item.id | string) + '/unsave') }}" class="govuk-!-margin-bottom-6">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <input type="hidden" name="redirect_to" value="saved">
            <button class="govuk-button govuk-button--secondary govuk-!-margin-bottom-0" data-module="govuk-button">{{ t("govuk_alpha_commerce.saved.remove") }}</button>
          </form>
        {% endif %}
      {% endfor %}
    </div>
  {% endif %}
{% endblock %}
