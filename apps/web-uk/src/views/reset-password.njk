{% extends "layouts/base.njk" %}

{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}

{% block mainContent %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      {% if errors and errors.length %}
        {{ govukErrorSummary({
          titleText: "There is a problem",
          errorList: errors
        }) }}
      {% endif %}

      <h1 class="govuk-heading-xl">Set a new password</h1>

      <p class="govuk-body">
        Enter your new password below.
      </p>

      <form method="post" action="{{ formAction or '/reset-password' }}" novalidate>
        <input type="hidden" name="_csrf" value="{{ csrfToken }}">
        <input type="hidden" name="token" value="{{ resetToken }}">

        {{ govukInput({
          label: {
            text: "New password",
            classes: "govuk-label--m"
          },
          hint: {
            text: "At least 12 characters. A memorable passphrase is stronger than a short complex one."
          },
          id: "password",
          name: "password",
          type: "password",
          autocomplete: "new-password",
          errorMessage: { text: fieldErrors.password } if fieldErrors.password else undefined,
          attributes: {
            "minlength": "12",
            "aria-describedby": "password-hint password-strength-msg"
          }
        }) }}
        <p id="password-strength-msg" class="govuk-body-s govuk-!-margin-top-2" aria-live="polite"></p>
        <script src="/js/password-strength.js" defer></script>

        {{ govukInput({
          label: {
            text: "Confirm new password",
            classes: "govuk-label--m"
          },
          id: "password_confirmation",
          name: "password_confirmation",
          type: "password",
          autocomplete: "new-password",
          errorMessage: { text: fieldErrors.password_confirmation } if fieldErrors.password_confirmation else undefined
        }) }}

        {{ govukButton({
          text: "Reset password"
        }) }}
      </form>

      <p class="govuk-body govuk-!-margin-top-6">
        <a href="/login" class="govuk-link">Back to sign in</a>
      </p>

    </div>
  </div>
{% endblock %}
