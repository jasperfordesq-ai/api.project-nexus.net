{# Copyright © 2024–2026 Jasper Ford #}
{# SPDX-License-Identifier: AGPL-3.0-or-later #}
{# Author: Jasper Ford #}
{# See NOTICE file for attribution and acknowledgements. #}

{% extends "layouts/base.njk" %}

{% block beforeContent %}
  {{ super() }}
  <a class="govuk-back-link" href="{{ urlFor('/reviews') }}">{{ t("govuk_alpha_blogreviews.reviews_list.back_to_reviews") }}</a>
{% endblock %}

{% block mainContent %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <span class="govuk-caption-xl">{{ t("govuk_alpha_blogreviews.reviews_list.caption") }}</span>
      <h1 class="govuk-heading-xl">{{ t("govuk_alpha_blogreviews.reviews_list.title") }}</h1>
      <p class="govuk-body-l">{{ t("govuk_alpha_blogreviews.reviews_list.description") }}</p>

      <form method="get" action="{{ urlFor('/reviews/list') }}" class="govuk-!-margin-bottom-6"><fieldset class="govuk-fieldset"><legend class="govuk-fieldset__legend govuk-fieldset__legend--s">{{ t("govuk_alpha_blogreviews.reviews_list.tab_legend") }}</legend><div class="govuk-radios govuk-radios--inline"><div class="govuk-radios__item"><input class="govuk-radios__input" id="tab-received" name="tab" type="radio" value="received"{% if reviewsTab == 'received' %} checked{% endif %}><label class="govuk-label govuk-radios__label" for="tab-received">{{ t("govuk_alpha_blogreviews.reviews_list.received_tab") }}</label></div><div class="govuk-radios__item"><input class="govuk-radios__input" id="tab-given" name="tab" type="radio" value="given"{% if reviewsTab == 'given' %} checked{% endif %}><label class="govuk-label govuk-radios__label" for="tab-given">{{ t("govuk_alpha_blogreviews.reviews_list.given_tab") }}</label></div></div></fieldset><button class="govuk-button govuk-button--secondary govuk-!-margin-top-2">{{ t("govuk_alpha_blogreviews.reviews_list.show") }}</button></form>

      {% if reviewsItems.length %}
        <ul class="nexus-alpha-card-list">
          {% for review in reviewsItems %}
            <li class="nexus-alpha-card">
              <h2 class="govuk-heading-m govuk-!-margin-bottom-1">{{ t("govuk_alpha_blogreviews.reviews_list.for_label" if review.direction == "given" else "govuk_alpha_blogreviews.reviews_list.by_label", { name: review.otherName }) }}</h2>
              <p class="govuk-body-s nexus-alpha-meta govuk-!-margin-bottom-1">{{ t("govuk_alpha_blogreviews.reviews_list.rating_label", { value: review.rating }) }}{% if review.createdAt %} - {{ review.createdAt | formatDate }}{% endif %}</p>
              {% if review.comment %}
                <p class="govuk-body">{{ review.comment }}</p>
              {% endif %}
              {% if review.id %}
                <p class="govuk-body-s govuk-!-margin-bottom-0"><a class="govuk-link" href="{{ urlFor('/reviews/' + (review.id | string) + '/comments') }}">{{ t("govuk_alpha_blogreviews.reviews_list.moderate_link") }}</a></p>
              {% endif %}
              {% if review.id and review.canDelete %}
                <details class="govuk-details govuk-!-margin-top-3 govuk-!-margin-bottom-0" data-module="govuk-details">
                  <summary class="govuk-details__summary">
                    <span class="govuk-details__summary-text">{{ t("polish_members.review_delete_toggle") }}</span>
                  </summary>
                  <div class="govuk-details__text">
                    <p class="govuk-body">{{ t("polish_members.review_delete_confirm") }}</p>
                    <form method="post" action="{{ urlFor('/reviews/' + (review.id | string) + '/delete') }}">
                      <input type="hidden" name="_csrf" value="{{ csrfToken }}">
                      <button class="govuk-button govuk-button--warning govuk-!-margin-bottom-0" data-module="govuk-button" type="submit">{{ t("polish_members.review_delete_button") }}</button>
                    </form>
                  </div>
                </details>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      {% else %}
        <p class="govuk-body">{{ t("govuk_alpha_blogreviews.reviews_list.given_empty" if reviewsTab == "given" else "govuk_alpha_blogreviews.reviews_list.received_empty") }}</p>
      {% endif %}

      {% if reviewsHasMore and loadMoreHref %}
        <p class="govuk-body govuk-!-margin-top-6"><a class="govuk-link" href="{{ urlFor(loadMoreHref) }}">{{ t("govuk_alpha_blogreviews.reviews_list.load_more") }}</a></p>
      {% endif %}
    </div>
  </div>
{% endblock %}
