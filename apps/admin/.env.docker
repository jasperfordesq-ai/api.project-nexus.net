# When running inside Docker via the main compose.yml, the API is reachable
# on the internal network as http://api:8080
# When running standalone (apps/admin/compose.yml), use http://localhost:5080
VITE_API_URL=http://api:8080
