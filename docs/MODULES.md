# Laravel-to-.NET Module Map

Last reviewed: 2026-07-09

Laravel source of truth: `C:\platforms\htdocs\staging`.

React frontend source of truth:
`C:\platforms\htdocs\staging\react-frontend`. The ASP.NET repo copy at
`apps/react-frontend/` is legacy/frozen and must not be used as the forward
development target unless the user explicitly approves that frontend work.

Status values:

- **Mapped**: .NET has an identifiable implementation surface, but parity still
  needs contract/workflow verification.
- **Partial**: .NET has some implementation, but Laravel has significant
  additional controllers, services, routes, frontend, or data model.
- **Gap**: no meaningful .NET implementation found in the source scan.

| Laravel module | Laravel source paths | .NET target paths | Status |
| --- | --- | --- | --- |
| Admin | `app/Http/Controllers/Api/Admin*`, `react-frontend/src/admin`, `docs/modules/admin.md` | `src/Nexus.Api/Controllers/Admin*`; ASP.NET must satisfy the Laravel React admin API contract. Admin user list/detail/update now matches Laravel React `/api/v2/admin/users*` data/meta envelopes, tenant scoping, search/sort/pagination, and core user fields; create now returns the Laravel React HTTP 201 data/meta envelope with validation errors; password/reset/welcome helper routes now match Laravel React data envelopes; user consents now return tenant-scoped Laravel React `data[]` rows and `errors[]` not-found envelopes; user CSV import now accepts Laravel React multipart `csv_file` uploads and returns `data.imported/skipped/errors/total_rows`; badge add/remove/recheck now returns Laravel React data envelopes and refreshed `data.badges[]`; impersonation now returns Laravel React `data.token/user_id/user_name/tenant_id/tenant_slug`; admin user-edit super-admin/global-super-admin toggles now accept Laravel React `{ grant }` payloads and return Laravel data envelopes; super-admin panel grant/revoke, user movement, bulk operations, audit reads, federation overview, system-controls, jwt-status, lockdown, whitelist, partnership, and tenant-feature routes now return Laravel React data envelopes and persist/read backend state; admin impact-report GET/config PUT now return Laravel React `data.sroi/health/timeline/config` and config update envelopes; admin CMS pages now cover Laravel React `/api/v2/admin/pages` list/detail/create/update/delete payloads including `status`, numeric menu flags, and Laravel-only metadata via tenant config; admin matching config GET/PUT now returns and persists the Laravel React `SmartMatchingConfig` data envelope with weights, proximity bands, gates, pillars, adjustments, and AI flags; approve/suspend/ban/reactivate/delete/reset-2FA now matches Laravel React `/api/v2/admin/users/{id}/*` action envelopes, tenant scoping, and core state changes; bulk approve/suspend matches Laravel React `user_ids`/`reason` payloads and shared bulk-result envelopes; system cron job list/manual-run now covers Laravel React `/api/v2/admin/system/cron-jobs` and `/api/v2/admin/system/cron-jobs/{id}/run` definition-list/manual-trigger envelopes while real scheduler side effects remain a deeper backend gap. The old `apps/react-frontend/src/admin` copy is legacy reference only. | Partial |
| AI chat | `app/Services/AI`, `docs/modules/ai-chat.md` | `src/Nexus.Api/Services/*Ai*`, `src/Nexus.Api/Controllers/*Ai*` | Mapped |
| Blog and resources | `docs/modules/blog-and-resources.md`, `app/Services/*Blog*`, resource routes | `AdminBlogController`, resources/admin compatibility controllers | Partial |
| Connections and reviews | `docs/modules/connections-and-reviews.md`, connection/review services | connection, endorsement, review controllers/services | Mapped |
| Courses | `docs/modules/courses.md`, `CourseController.php`, `CourseEnrollmentController.php`, `CourseContentController.php`, `CourseQuizController.php`, `CourseCohortController.php`, `CourseGroupController.php`, `CourseDiscussionController.php`, Laravel React `src/lib/api/courses.ts` | `CoursesCompatibilityController.cs`, `CoursesCompatibilityService.cs`, `AdminCoursesController.cs`, `AdminCoursesService.cs`; compatibility state currently stored under tenant-scoped `TenantConfig` | Partial |
| Events | `docs/modules/events.md`, `EventsController`, event services | `EventsController`, admin event controllers | Mapped |
| Gamification | `docs/modules/gamification.md`, gamification services/controllers | gamification controllers/services and locale files | Mapped |
| Goals and impact | `docs/modules/goals-and-impact.md`, goals/regional impact services | goals controllers/services; regional analytics missing | Partial |
| Groups | `docs/modules/groups.md`, group services/controllers | groups controllers/services | Mapped |
| Ideation challenges | `docs/modules/ideation-challenges.md`, `IdeationChallengeService.php`, `CampaignService.php`, `ChallengeCategoryService.php`, `ChallengeTagService.php`, `ChallengeTemplateService.php`, `TeamTaskService.php`, `TeamDocumentService.php`, `GroupChatroomService.php`, Laravel React `CreateChallengePage.tsx`, `ChallengeDetailPage.tsx`, `IdeaDetailPage.tsx`, `CampaignsPage.tsx`, `CampaignDetailPage.tsx`, `OutcomesDashboardPage.tsx`, `TeamTasks.tsx`, `TeamDocuments.tsx`, `TeamChatrooms.tsx` | ideation controllers/services; `POST /api/v2/ideation-challenges` now accepts the Laravel React create payload, persists a real `Challenge`, returns HTTP 201 `success/data`, writes the challenge feed-author compatibility key used by admin feed moderation, `PUT/GET /api/v2/ideation-challenges/{id}` now preserve Laravel React edit-form metadata through tenant config, `PUT /api/v2/ideation-challenges/{id}/status` now enforces Laravel's admin-only lifecycle transition contract and persists status for detail reads, `POST /api/v2/ideation-challenges/{id}/favorite` now toggles Laravel React `data.favorited/favorites_count` and detail `is_favorited/favorites_count`, `POST /api/v2/ideation-challenges/{id}/duplicate` now returns a Laravel draft copy with HTTP 201 and copied edit metadata, `GET/PUT /api/v2/ideation-challenges/{id}/outcome` now persist/read Laravel React outcome modal fields, `GET /api/v2/ideation-outcomes/dashboard` now returns React summary counters and outcome rows, `GET/POST /api/v2/ideation-campaigns`, `GET/PUT/DELETE /api/v2/ideation-campaigns/{id}`, `POST /api/v2/ideation-campaigns/{id}/challenges`, and `DELETE /api/v2/ideation-campaigns/{id}/challenges/{challengeId}` now cover Laravel React campaign CRUD/link behavior with 201/204 status codes and linked challenge rows, ideation category/tag/template bootstrap `/api/v2` aliases now return Laravel React `success/data` envelopes and field names for create/list screens, explicit idea/comment/media `/api/v2` routes now cover submit/list/drafts/detail/update/draft/status/delete plus JSON link-media create/list/delete and comment create/list/delete shapes, team-task `/api/v2` aliases now cover list/create/detail/update/delete/task-stats with Laravel React envelopes, statuses, priorities, stats, and 204 deletes, team-document `/api/v2` aliases now cover upload/list/delete with persisted upload refresh, Laravel document fields, 201 uploads, and 204 deletes, team-chatroom `/api/v2` aliases now cover channel list/create/delete, message list/create/delete, pin/unpin, pinned reads, 201 creates/pins, and 204 deletes, and `DELETE /api/v2/ideation-challenges/{id}` now returns Laravel's HTTP 204 and removes the challenge/feed/favorite/outcome compatibility state | Partial; exact ideation/taxonomy/template/team collaboration schemas, side effects, private-channel fidelity, real-time broadcasts, and broader runtime frontend smoke coverage remain gaps |
| Identity verification | `docs/modules/identity-verification.md`, `app/Services/Identity/*Provider.php`, `RegistrationPolicyController`, `TenantProviderCredentialService.php` | registration engine plus Mock, Stripe Identity, Veriff, Onfido, Jumio, and iDenfy adapters; React admin read/write compatibility contracts; encrypted tenant provider credential store | Partial |
| Tenant SSO / OIDC | `AdminSsoProvidersController.php`, `SsoOidcService.php`, `tenant_sso_providers` migration | `AdminSsoProvidersController.cs`, `TenantSsoProviderService.cs`, `TenantSsoProvider.cs`, `tenant_sso_providers` migration | Partial |
| Jobs | `docs/modules/jobs.md`, jobs controllers/services | jobs controllers/services | Mapped |
| Listings | `docs/modules/listings.md`, listings services/controllers | listings controllers/services | Mapped |
| Marketplace | `docs/modules/marketplace.md`, `Marketplace*Controller.php`, `MerchantOnboardingController.php`, `MarketplacePaymentController.php`, `MarketplaceAiController.php`, `MarketplaceCommunityDeliveryController.php`, `MarketplaceGroupController.php`, `MarketplacePickupSlotController.php`, `Marketplace*Service.php`, marketplace models, Laravel React marketplace pages/components including `StripeOnboardingPage.tsx` | `MarketplaceController.cs`, `AdminMarketplaceController.cs`, `MarketplaceService.cs`, `MarketplaceEntities.cs`, `V15SocialCompatibilityController.cs`, `MemberParityController.cs`, `MediaThumbnailCompatibilityController.cs`; merchant-onboarding wizard plus image upload, public uploaded-media thumbnail route, seller profile avatar/cover upload, seller Stripe onboarding status/start shape, category list counts/template empty envelope, listing browse/map-search/detail/save/report/create/edit/media upload, public seller profile/listings, group marketplace listings/stats, collections/saved-searches, seller dashboard/my-listings/renew/delete, seller shipping-options CRUD, promotion selector, marketplace offer create/list/action, seller coupon CRUD plus admin dashboard/listing moderation actions and bulk reject/seller/coupon oversight, BuyNow coupon/order/payment-intent/listing pickup-slot reads, owner-only AI auto-reply request shape, order history/ship/confirm-delivery/rating, seller pickup-slot, pickup reservation/QR scan, and community-delivery React contracts are covered by `MarketplaceControllerTests` plus marketplace unit tests | Partial |
| Members and GDPR | `docs/modules/members-and-gdpr.md`, `UsersController.php`, `MemberAvailabilityController.php`, `UserInsuranceController.php`, `SocialAuthController.php`, `BlockUserController.php`, `SkillTaxonomyController.php`, `SafeguardingMemberController.php`, `SubAccountController.php`, member/GDPR/blocking/skill/safeguarding/sub-account services, Laravel React `ThemeContext.tsx`, `SettingsPage.tsx`, `BlockedUsersPage.tsx`, `ConnectedAccountsTab.tsx`, `SubAccountsManager.tsx`, `PrivacyTab.tsx`, `SkillSelector.tsx`, `SafeguardingTab.tsx`, and `AvailabilityGrid.tsx` | user, GDPR, consent, deletion controllers/services; ThemeContext `GET /api/v2/users/me`, `PUT /theme`, and `PUT /theme-preferences` contracts are covered by `UsersControllerTests`; settings privacy/feed/translation, notifications, consent, GDPR-request, sessions, and default-disabled OAuth connected-accounts discovery/link behavior are covered by `LaravelReactFrontendContractTests`; linked-accounts settings `GET/POST /api/v2/users/me/sub-accounts`, `GET /api/v2/users/me/parent-accounts`, `PUT /approve`, `PUT /permissions`, and `DELETE` now accept Laravel React email requests, return relationship rows, child approval, parent permission updates, and remove flows with focused `LaravelReactSubAccountsCompatibilityTests` coverage; availability-grid `GET/PUT /api/v2/users/me/availability` and `GET /api/v2/users/{id}/availability` contracts are covered by `AvailabilityControllerTests`; member insurance `GET/POST /api/v2/users/me/insurance` now accepts Laravel React multipart `certificate_file` uploads and returns Laravel certificate field names with focused `InsuranceControllerTests` coverage; blocked-users settings `POST/GET/DELETE /api/v2/users/{id}/block`, `GET /api/v2/users/blocked`, and `GET /api/v2/users/{id}/block-status` now persist tenant-scoped `user_blocks` rows and return Laravel React blocked-user/envelope shapes with focused `LaravelReactBlockedUsersCompatibilityTests` coverage; skills settings `GET/POST/PUT/DELETE /api/v2/users/me/skills` now accept custom `skill_name` payloads, persist .NET `skills`/`user_skills`, and return Laravel React list/delete envelopes with focused `LaravelReactSkillsCompatibilityTests` coverage; safeguarding settings `GET /api/v2/safeguarding/my-preferences` and `POST /api/v2/safeguarding/revoke` now persist `user_safeguarding_preferences`, return Laravel React `preferences/count` payloads, and soft-revoke by `option_id` with focused `LaravelReactSafeguardingCompatibilityTests` coverage | Mapped |
| Messaging | `docs/modules/messaging.md`, message services, Pusher auth, Laravel React `PresenceContext.tsx`, `PusherContext.tsx`, `MessagesPage.tsx`, `ConversationPage.tsx`, `MessageBubble.tsx`, `NotificationsContext.tsx` | messages controllers/services, SignalR/RabbitMQ surfaces; `V15SocialCompatibilityController` now exposes the Laravel React presence context routes (`/api/v2/presence/heartbeat`, `/online-count`, `/users`, `/status`, `/privacy`) with Laravel-style envelopes and tenant-scoped persistence; `NotificationPollingController` now exposes the Laravel React `/api/v2/realtime/config` bootstrap envelope and `V15SocialCompatibilityController` owns `/api/pusher/auth` validation plus configured raw Pusher signing for tenant/user private channels and tenant presence channels; `CompatibilityAliasController` now accepts the Laravel React `POST /api/v2/messages/typing` `{ to_user_id, is_typing }` payload; `ReactFrontendCompatibilityController` now returns the Laravel React `GET /api/v2/messages/restriction-status` composer-gating envelope; `MessagesController` now owns the non-ambiguous `GET /api/v2/messages` inbox list alias, the non-ambiguous `POST /api/v2/messages` send alias including Laravel React multipart `attachments[]` form-data and attachment-only sends, the Laravel React `GET /api/v2/messages/{otherUserId}` thread load contract with `success/data/meta.conversation`, `GET /api/v2/messages/unread-count` with `success/data.count`, and `PUT /api/v2/messages/{otherUserId}/read` with `success/data.marked_read`; `MemberParityController` now covers Laravel React `POST /api/v2/messages/voice` multipart `voice_message` sends and returns stored-message content for `POST /api/v2/messages/{messageId}/translate` with `success/data.translated_text` while toggling `POST /api/v2/messages/{messageId}/reactions` with Laravel's `success/data.action` contract; archived conversation storage, provider-backed translation and voice transcription quality, full reaction aggregate/schema parity, and end-to-end Pusher broadcast smoke coverage remain deeper parity gaps | Mapped |
| Monetization | `docs/modules/monetization.md`, `DonationPaymentController.php`, `StripeDonationService.php`, `AdminBillingController.php`, subscriptions, coupons, ads, Laravel React `DonationCheckout.tsx` / `DonationReceipt.tsx` / `admin/api/billingApi.ts` | `MiscParityController.cs`, `AdminExplicitParityController.cs`, `MoneyDonation`, `SubscriptionPlan`, `UserSubscription`, admin donation/member-premium compatibility routes; donation checkout payment-intent and receipt contracts are covered by `LaravelReactDonationContractTests.cs`; `/api/v2/billing/plans` now returns tenant-scoped database-backed Laravel React `Plan` rows with numeric ids, generated slugs, prices, feature arrays, and `is_active`, covered by `AdminV2RouteAliasRuntimeTests`; `/api/v2/admin/billing/subscription` now returns the singular Laravel React `SubscriptionDetails` object for the latest tenant subscription; `/api/v2/admin/billing/checkout` now activates zero-price plans with the Laravel React `activated`/`checkout_url` envelope; `/api/v2/admin/billing/invoices` now projects subscription-backed rows with the Laravel React invoice fields; `/api/v2/admin/billing/portal` now returns Laravel's `NO_SUBSCRIPTION` error envelope when no Stripe customer is available; `/api/v2/admin/billing/upgrade-request` now returns Laravel `data.sent=true` and records the tenant upgrade request in `AuditLog`, covered by `AdminExplicitParityControllerTests`; paid Stripe checkout/real portal/Stripe invoice retrieval and provider-backed upgrade email workflows remain gaps | Partial |
| Notifications | `docs/modules/notifications.md`, notification/email/push services | notification, push, email services/controllers | Mapped |
| Organisations | `docs/modules/organisations.md`, organisation services/controllers | organisation controllers/services | Mapped |
| Podcasts | `docs/modules/podcasts.md`, `PodcastController.php`, `AdminPodcastController.php`, podcast routes, Laravel React `src/lib/api/podcasts.ts`, `src/admin/modules/podcasts/PodcastsAdmin.tsx` | `PodcastsCompatibilityController.cs`, `PodcastsCompatibilityService.cs`; compatibility state currently stored under tenant-scoped `TenantConfig`; Laravel React multipart `audio` episode upload is covered for `POST /api/v2/podcasts/{showId}/episodes` | Partial |
| Search | `docs/modules/search.md`, Meilisearch and SQL fallback | search/semantic search controllers/services | Mapped |
| Social feed | `docs/modules/social-feed.md`, `SocialController.php`, `FeedSocialController.php`, `CommentsController.php`, `ReactionController.php`, `AdminFeedController.php`, `FeedRankingService.php`, `ShareService.php`, `CommentService.php`, `ReactionService.php`, `MentionController.php`, feed/poll/story services, Laravel React `useFeedTracking.ts`, `useFeedImpression.ts`, `ShareButton.tsx`, `useSocialInteractions.ts`, `MentionInput.tsx`, feed/profile/detail/group reaction calls, `adminModeration.*` feed calls | feed, poll, comments, moderation controllers/services; `V15SocialCompatibilityController` now covers Laravel React feed impression/click tracking routes with `data.recorded` envelopes and validation errors, `/api/v2/feed/hashtags/trending`, `/api/v2/feed/hashtags/search`, and `/api/v2/feed/hashtags/{tag}` tenant-scoped/limit-aware Laravel `success/data/meta` rows, `/api/v2/feed/posts/{id}/hide` and `/api/v2/feed/posts/{id}/not-interested` route-id plus optional `type` payloads for post and non-post feed targets, `POST /api/v2/feed/users/{id}/mute` target-user validation and Laravel `data.muted/user_id` envelope, `DELETE /api/v2/feed/posts/{id}` plus `POST /api/v2/feed/posts/{id}/delete` owner-only post deletion with Laravel `data.deleted/id` and 403/404 errors, `/api/v2/feed/posts/{id}/report` and `/api/v2/feed/items/{type}/{id}/report` report payloads with Laravel `data.reported/target_type/target_id` envelopes, `/api/v2/feed/like` and `/api/social/like` post/non-post like toggles through Laravel-compatible `ContentLike`/`likes` storage while mirroring post rows into legacy `PostLike`, `/api/social/likers` Laravel React liker-result envelopes for post and non-post feed targets, legacy `/api/social/comments` fetch/submit action payloads for generic threaded comments, legacy `/api/social/reaction` comment-reaction toggles, legacy `/api/social/mention-search` `success/data.users` fallback, plus polymorphic `/api/v2/shares` share/toggle/delete and sharer-list envelopes for post and non-post feed items; `CommentsV2Controller`, `ThreadedCommentService`, `CompatibilityAliasController`, and `CommentReaction` now cover `/api/v2/comments` create/reply/list/edit/delete plus comment reaction toggle shapes for feed posts; `MiscParityController` and `ContentReaction` now cover `/api/v2/reactions` polymorphic toggle/show/reactor-list shapes for post and listing feed items; `MemberParityController.MentionSearch` now covers Laravel React `/api/v2/mentions/search` autocomplete shape, active tenant filtering, caller exclusion, one-character queries, `limit`, and connection-first ordering while preserving .NET surname privacy; `PostShare` now carries Laravel-compatible polymorphic share identity; `AdminFeedController.cs` now covers Laravel React `/api/v2/admin/feed/posts` list/detail/hide/delete/stats contracts with snake_case envelopes for post, listing, event, poll, goal, job, challenge, volunteer, blog, and discussion source types while remaining Laravel unified `feed_activity`, `feed_hidden`, `feed_impressions`, `feed_clicks`, native `reports` schema fidelity, HMAC cursor signing, richer FeedService item projection parity, share/repost feed rows, ranking side effects, mention extraction/notification side effects, and runtime frontend smoke coverage still need work | Partial |
| Volunteering | `docs/modules/volunteering.md`, volunteering services/admin pages | volunteer long-tail services/controllers; admin expense receipt download now covers Laravel React `GET /api/v2/admin/volunteering/expenses/{id}/receipt` with tenant-scoped uploaded-file streaming and not-found errors | Partial |
| Wallet and exchanges | `docs/modules/wallet-exchanges.md`, wallet/exchange services | wallet/exchange services/controllers | Mapped |
| Caring Community | `app/Services/CaringCommunity`, `CaringCommunity*`, `AdminCaringCommunityController::workflow/tandemSuggestions/dismissTandemSuggestion/assistedOnboarding`, `MunicipalSurveyController.php`, `TrustTierController.php`, `WarmthPassController.php`, `CaregiverApiController.php`, `VereinFederationMemberController.php`, KISS/municipal routes/admin pages | .NET has initial member/admin parity for emergency alerts, external integration backlog, federation peers plus member federation-directory, sub-regions, care providers, success stories, caregiver links plus burnout/schedule/request-on-behalf and cover-request reads/create/assign, public municipality events-calendar default/code routes, admin assisted onboarding, admin workflow dashboard read plus policy update, review assignment, review decision, and review escalation routes, admin tandem suggestions read/dismiss routes with suppression log, project announcements, category coefficients, commercial boundary, municipal copilot, data-quality, civic digest member digest/prefs and admin cadence, disclosure packs, integration showcase, favours including member offer-favour, forecasts, KPI baselines, pilot scoreboard, recipient circle, regional-points admin/member summary/history/transfer/marketplace quote/redeem, research reads/writes, role-preset status/install, trust-tier config/recompute/member-breakdown, warmth-pass member/admin reads, safeguarding reads plus member report submission/report assignment/escalation/note/status actions, SLA/support reads plus admin support-relationship create/update/hour logging, admin and member Verein member import/preview plus admin assignment, member relationship lifecycle, member request-help and voice prefill, launch readiness, operating policy, member statements, municipal ROI, nudge analytics/config plus tandem-candidate dispatch, paper onboarding, lead nurture, loyalty, municipality feedback, municipality surveys, hour estates, hour transfers, hour-gift inbox/sent reads plus send/accept/decline/revert mutations, KISS Treffen member list/detail reads plus admin upsert/minutes mutations, Caring Community Markt member feed, invite codes, and isolated-node gates. Evidence includes corresponding controllers/services plus represented tables/settings such as `caring_federation_peers`, `caring_cover_requests`, `caring_help_requests`, `caring_municipality_feedback`, `caring_tandem_suggestion_log`, `caring_trust_tier_config`, `caring_hour_gifts`, `caring_kiss_treffen`, `caring_smart_nudges`, `municipality_surveys`, `municipality_survey_questions`, `municipality_survey_responses`, `verein_federation_consents`, regional-points/research/loyalty tables, `TenantConfig` civic digest/user prefs and workflow policy keys, listing geo/image fields for the Markt feed, `CaringCommunityWorkflowService`, `CaringCommunityRolePresetService`, `CaringSafeguardingService`, `CaringSupportRelationshipService`, `CaringCommunityVereineAdminService`, `CaringTandemMatchingService`, `CaringNudgeAnalyticsService`, `CaringHourGiftService`, `KissTreffenService`, `CaringCommunityMarktService`, `MunicipalityEventsCalendarController`, `AdminCaringCommunityAssistedOnboardingController`, `AdminCaringCommunitySupportController`, `AdminCaringCommunityVereineController`, `AdminCaringCommunityKissTreffenController`, and shared columns/settings. Warmth-pass category derivation until `caring_help_requests.category_id` exists, non-tandem nudge trigger parity, civic digest email delivery/background claims, frontend survey/trust-tier/warmth-pass/workflow/civic-digest routes, accessible coverage, and many deeper caring services/routes still need parity. | Partial |
| Verein / Clubs | `app/Http/Controllers/Api/Verein`, `app/Services/Verein`, `ClubsApiController.php` | `VereineParityController.cs`, `AdminCaringCommunityVereineController.cs`, `CaringCommunityVereineAdminService.cs`, focused auth/admin-assignment tests | Partial |
| Regional Analytics | `routes/regional-analytics-routes.txt`, `app/Services/RegionalAnalytics*`, `Api/SuperAdmin/RegionalAnalyticsAdminController.php`, regional analytics schema migrations | EF entities/configuration/migration/tests now cover `regional_analytics_subscriptions`, `regional_analytics_reports`, `regional_analytics_access_log`, and `regional_analytics_cache`; Laravel React super-admin subscription management/report queue/access-log calls now have backend compatibility routes and focused tests; tenant-admin overview, heatmap, demand/supply, demographics, engagement trends, volunteer breakdown, help-request analytics, export JSON, and cache invalidation routes now have backend compatibility routes and focused tests; partner report download now returns a valid minimal PDF from report metadata/payload instead of placeholder text | Partial: billing, stored-file fidelity, scheduled/monthly job workflows, deeper service parity, and runtime smoke coverage remain gaps |
| National KISS | `NationalKissDashboardController.php`, `NationalKissDashboardService.php`, Laravel React `NationalKissDashboardPage.tsx` | `NationalKissDashboardController.cs` covers summary, comparative, trend, and cooperatives backend contracts with privacy-preserving buckets and focused regression tests | Partial: Laravel `tenants.tenant_category`, `national.kiss_dashboard.view` permission parity, caching, runtime smoke coverage, and deeper service equivalence remain gaps |
| Partner API and portal | `app/Http/Controllers/Api/PartnerApi`, `app/Services/PartnerApi`, `react-frontend/src/partners` | `AdminApiPartnersController`, API partner entity/service | Partial |
| Accessible frontend | `accessible-frontend/`, `routes/govuk-alpha.php`, `routes/govuk-alpha-parity/*` | `apps/web-uk/`; see `docs/FRONTEND_PARITY.md` | Partial |

## Notes

- The Laravel repo has 24 curated module guides in `docs/modules/`; those guides
  should be read before implementing each corresponding .NET module.
- Members/GDPR FADP consent-banner calls now have focused backend contract
  coverage: `POST /api/v2/me/fadp/consent` accepts Laravel React
  `action=granted|withdrawn`, returns `data.recorded/consent_type/action`, and
  `GET /api/v2/me/fadp/consent-history` projects Laravel `action` rows.
- Monetization feed-ad tracking now covers the Laravel React
  `FeedAdCard.tsx` calls: `POST /api/v2/ads/impression` returns
  `data.impression_id`, and `POST /api/v2/ads/impression/{id}/click` returns
  `data.ok=true`.
- Social feed profile-card stats now cover `GET /api/v2/me/stats` for
  `ProfileCardWidget.tsx`, returning `listings_count`, `given_count`,
  `received_count`, `offers_count`, `requests_count`, and `wallet_balance`.
- "Mapped" does not mean complete. It means the .NET repo has enough surface to
  start a detailed contract/workflow parity audit.
- Full parity requires the gap register in `LARAVEL_PARITY_MAP.md` to close, not
  merely for a controller name to exist.
