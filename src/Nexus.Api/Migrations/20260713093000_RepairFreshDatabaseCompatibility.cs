// Copyright (c) 2024-2026 Jasper Ford
// SPDX-License-Identifier: AGPL-3.0-or-later
// Author: Jasper Ford
// See NOTICE file for attribution and acknowledgements.

using Microsoft.EntityFrameworkCore.Infrastructure;
using Microsoft.EntityFrameworkCore.Migrations;
using Nexus.Api.Data;

#nullable disable

namespace Nexus.Api.Migrations;

[DbContext(typeof(NexusDbContext))]
[Migration("20260713093000_RepairFreshDatabaseCompatibility")]
public partial class RepairFreshDatabaseCompatibility : Migration
{
    protected override void Up(MigrationBuilder migrationBuilder)
    {
        migrationBuilder.Sql("""
            CREATE TABLE IF NOT EXISTS "XpShopRedemptions" (
                "Id" integer GENERATED BY DEFAULT AS IDENTITY,
                "TenantId" integer NOT NULL,
                "UserId" integer NOT NULL,
                "ItemId" text NOT NULL,
                "ItemName" text NOT NULL,
                "XpSpent" integer NOT NULL,
                "RedeemedAt" timestamp with time zone NOT NULL,
                "IsActive" boolean NOT NULL,
                "ExpiresAt" timestamp with time zone NULL,
                CONSTRAINT "PK_XpShopRedemptions" PRIMARY KEY ("Id"),
                CONSTRAINT "FK_XpShopRedemptions_tenants_TenantId"
                    FOREIGN KEY ("TenantId") REFERENCES tenants ("Id") ON DELETE CASCADE,
                CONSTRAINT "FK_XpShopRedemptions_users_UserId"
                    FOREIGN KEY ("UserId") REFERENCES users ("Id") ON DELETE CASCADE
            );

            CREATE INDEX IF NOT EXISTS "IX_XpShopRedemptions_TenantId"
                ON "XpShopRedemptions" ("TenantId");
            CREATE INDEX IF NOT EXISTS "IX_XpShopRedemptions_UserId"
                ON "XpShopRedemptions" ("UserId");

            ALTER TABLE push_subscriptions
                ADD COLUMN IF NOT EXISTS "P256dh" character varying(200) NULL;
            ALTER TABLE push_subscriptions
                ADD COLUMN IF NOT EXISTS "Auth" character varying(64) NULL;
            """);
    }

    protected override void Down(MigrationBuilder migrationBuilder)
    {
        throw new NotSupportedException(
            "RepairFreshDatabaseCompatibility is forward-only because the repaired table and " +
            "Web Push key columns may contain member data. Apply a reviewed forward migration instead.");
    }
}
