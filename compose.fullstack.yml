# QUARANTINED LEGACY FULL-STACK LOCAL TOPOLOGY - NO EXECUTABLE SERVICES
#
# The former duplicate full-stack definition had stale ports and startup
# behavior. Compose profiles did not provide a real quarantine because an
# explicitly named service bypassed them. The historical definition remains
# available in Git before the 2026-07-15 documentation/operations audit. Use
# the maintained local paths in docs/system/LOCAL_DEVELOPMENT.md instead.

name: nexus-quarantined-fullstack-history
services: {}
