# =============================================================================
# Docker Environment Variables for Nexus Backend
# =============================================================================
# Copy this to .env for local Docker development (not committed to git)
#
# Usage:
#   cp .env.docker .env
#   # Edit .env with your values (generate a secure secret!)
#   docker compose up -d
#
# IMPORTANT: The .env file is gitignored. Never commit real secrets.
# =============================================================================

# JWT Secret - MUST be at least 32 characters
# Generate a secure random string for production:
#   openssl rand -base64 32
#   or: head -c 32 /dev/urandom | base64
#
# REPLACE THIS with your own secure secret:
JWT_SECRET=REPLACE_ME_WITH_A_SECURE_32_CHAR_SECRET

# Note: Database connection is configured in compose.yml to use Docker networking
# (Host=db) and does not need to be set here for Docker.
